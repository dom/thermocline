# Python bytecode and build artifacts
__pycache__/
*.pyc
*.pyo
*.pyd
*.egg-info/
*.egg
build/
dist/
.eggs/

# Virtualenvs
.venv/
venv/
env/

# Test / type-check / coverage artifacts
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# Editor / OS
.DS_Store
.vscode/
.idea/
*.swp

# uv lock — dev-only artifact (repository README: pip-install for end users; uv for dev)
uv.lock

# local tooling local permission rules — operator-specific, not version-controlled
local tool settings

# Internal workspace (kept locally, not for public repo)
.planning/
memory/
local tool workspace
