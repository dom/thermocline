---
phase: 01-thermocline-py-foundations
verified: 2026-05-08T08:20:02Z
status: gaps_found
score: 3/5 must-haves verified
overrides_applied: 0
gaps:
  - truth: "Round-trip via thermocline-py only — sign with the brine adapter and verify across roles — produces a Receipt; tampering invalidates."
    status: failed
    reason: "Two architectural defects break the cross-role round-trip required by SC2 and Phase 3 SC3: (1) BrineProvider.public_key() reads the private seed from the verifier's keystore — there is no public-key store, so a verifier role (e.g., a forge) cannot verify any signature without holding the signer's private seed, contradicting the spec's 'the public key is the node identity — share it freely' guarantee (BL-01); (2) Verifier.verify() reads `envelope.get('key_scheme')` from the top level, but every real envelope shape (Task / TaskResult / Job / JobResult) nests `key_scheme` under `dispatch_signature` / `receipt_signature` — every real signed envelope is rejected with `SchemeError('declared key scheme None does not match signature scheme \\'brine\\'')` (BL-02). Tamper detection itself works correctly when round-trip succeeds (returns None) — but the round-trip cannot run on real envelopes through the documented Verifier API."
    artifacts:
      - path: "thermocline/python/src/thermocline/identity.py"
        issue: "Lines 312-327 (`public_key`) and 354-402 (`verify`): public_key() pulls the seed from the keystore via `keyring.get_password(self._keyring_service, identity)` and derives the verify key from it. Verify() then calls public_key() — meaning the verifier must hold the signer's seed. Independent test confirmed: deleting the seed causes public_key() to raise IDENTITY_NOT_FOUND. Lines 209-234 (`Verifier.verify`): `declared = envelope.get('key_scheme')` — top-level lookup. Independent test with the real `task-pi-100-digits.json` fixture (with `dispatch_signature.key_scheme = 'brine'`) confirmed that Verifier.verify raises SchemeError because the top-level `key_scheme` is `None`."
    missing:
      - "A separate public-key store path on BrineProvider (e.g., `register_public_key(identity, verify_key)` writing to a `_PUBKEY_PREFIX + identity` keyring entry, plus `public_key()` reading from that store) so a verifier role can hold only public keys."
      - "Behavioural test: a BrineProvider whose keystore contains ONLY a registered public key (no seed) successfully verifies a signature produced by a different BrineProvider on a different keystore."
      - "`Verifier.verify` (or a helper) reads `key_scheme` from the canonical nested location — `envelope['dispatch_signature']['key_scheme']` for `task` / `job` envelopes and `envelope['receipt_signature']['key_scheme']` for `task_result` / `job_result` envelopes — falling back to top-level only as a tolerated synthetic-test path."
      - "Regression test: a real Task envelope (e.g., loaded from `thermocline/conformance/valid/task-pi-100-digits.json` with `dispatch_signature.key_scheme = 'brine'`) plus a real Signature passed through `Verifier.verify` produces a Receipt — currently NO test does this, AT-C4 is referenced as 'wired in test_identity_dispatch' but the dispatch test only uses synthetic flat dicts (`{'envelope_id': '...', 'key_scheme': 'brine'}`)."
  - truth: "The IdentityProvider reference adapter, when configured for `brine`, refuses to start if `python-keyring` cannot reach the platform secure keystore (no fall-back to file/env-var storage)."
    status: failed
    reason: "BL-03 — the startup probe in `BrineProvider.__init__` checks `if 'fail' in backend_name.lower() or 'null' in backend_name.lower()`, where `backend_name = type(backend).__name__`. The actual production classes `keyring.backends.fail.Keyring` and `keyring.backends.null.Keyring` are both named `'Keyring'` — independently confirmed via `from keyring.backends import fail, null; print(fail.Keyring.__name__)` → `'Keyring'`. The substring check therefore PASSES for both production fail-mode backends, and `BrineProvider.__init__` proceeds — silently falling open in exactly the deployment scenarios IDENT-05 mandates be rejected (e.g., a Linux server with no D-Bus session, where keyring auto-selects null). The unit tests in `test_identity_keystore_required.py` exercise this only with hand-rolled `FailKeyring` / `NullKeyring` classes whose `__name__` does match the substring — masking the production behaviour. The Hypothesis property test for canonical-JSON round-trip stability does exist and passes; that half of the SC4 criterion is met."
    artifacts:
      - path: "thermocline/python/src/thermocline/identity.py"
        issue: "Lines 273-294: substring check on `type(backend).__name__` rather than on the qualified name `type(backend).__module__ + '.' + type(backend).__name__` or an `isinstance` check against `keyring.backends.fail.Keyring` / `keyring.backends.null.Keyring`."
      - path: "thermocline/python/tests/test_identity_keystore_required.py"
        issue: "Lines 55-87: tests use synthetic `FailKeyring` / `NullKeyring` classes whose `__name__` contains the matched substring; production classes do not. False sense of coverage."
    missing:
      - "Replace the substring heuristic with a direct identity check: `from keyring.backends import fail as _fail, null as _null; if isinstance(backend, (_fail.Keyring, _null.Keyring)): raise KeystoreUnavailableError(...)` (or include the module qualifier in the substring search)."
      - "Update `test_brine_refuses_to_start_with_fail_backend` and `…_null_backend` to import the real `keyring.backends.fail.Keyring` and `keyring.backends.null.Keyring` and assert the adapter rejects them — re-run on Linux/headless to confirm."
deferred: []
human_verification: []
---

# Phase 1: `thermocline-py` Foundations Verification Report

**Phase Goal:** Establish the foundation library every other suite component depends on. `thermocline-py` defines the envelope types (Pydantic v2), the single canonical-JSON path used for all signature input (RFC 8785), the `IdentityProvider` Protocol with a real PyNaCl-backed brine reference adapter (`python-keyring` for key material), and the JSON Schema artifacts that any third-party impl can validate against.

**Verified:** 2026-05-08T08:20:02Z
**Status:** gaps_found
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths (Phase 1 Success Criteria)

| #   | Truth (Success Criterion)                                                                                                                                                                                                | Status     | Evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | `pip install -e ./thermocline/python` succeeds; importing `thermocline` exposes envelope types, `canonicalize`, `IdentityProvider`, and the brine reference adapter.                                                     | ✓ VERIFIED | Independently confirmed: `pip install -e thermocline/python` on a fresh venv (Python 3.14) installs cleanly; `from thermocline import Task, TaskResult, Job, JobResult, ErrorEnvelope, ContentBlock, Sensitive, KeyScheme, SUPPORTED_VERSIONS, EnvelopeError, canonicalize, CanonicalizationError, IdentityProvider, BrineProvider, Verifier, Signature, Receipt, IdentityError, SchemeError, KeystoreUnavailableError` succeeds. Public `__all__` covers the surface. |
| 2   | Round-trip — build envelope, canonicalize, sign with brine, verify — produces `Receipt`; tampering invalidates.                                                                                                          | ✗ FAILED   | Round-trip succeeds **only** when the verifier holds the signer's private seed (BL-01: `public_key()` reads the seed). On real envelope shapes, `Verifier.verify` raises `SchemeError` because `key_scheme` is read from the wrong dict path (BL-02 — independently reproduced with `task-pi-100-digits.json`). Tamper detection itself works (returns None) when the round-trip is artificially completed via a shared keystore + flat-dict envelope. |
| 3   | JSON Schema files exist for `task`, `task_result`, `job`, `job_result`, `error`; valid fixtures validate; invalid fixtures fail.                                                                                         | ✓ VERIFIED | All 5 schemas present under `thermocline/schema/`; conformance tests `test_valid_pairs_parse_and_validate_against_schema` and `test_phase1_wired_invalid_fixtures_raise_expected_errors` pass; AT-C5/AT-C6 fixtures produce `UNSUPPORTED_VERSION` / `EXTRA_FIELD_FORBIDDEN` errors as documented in the manifest. `python -m thermocline.scripts.build_schemas --check` exits 0 (drift gate clean).                                                              |
| 4   | Brine adapter refuses to start without keystore; Hypothesis property test for canonical-JSON round-trip stability.                                                                                                       | ✗ FAILED   | Hypothesis property tests in `test_canonical_properties.py` exist and pass (round-trip stability, tamper detection, key-order normalization, list-order sensitivity, Sensitive[bytes] wire transparency). **However, IDENT-05 is broken in production** — BL-03 confirmed: `keyring.backends.fail.Keyring.__name__` = `'Keyring'` and `keyring.backends.null.Keyring.__name__` = `'Keyring'`; the substring check `'fail' in 'keyring'` / `'null' in 'keyring'` is False, so production fail/null backends are accepted. Adapter silently falls open. |
| 5   | `Receipt` cannot be constructed except via `IdentityProvider.verify`; direct `Receipt(...)` raises `TypeError` and `mypy --strict` rejects deliberate misuse.                                                            | ✓ VERIFIED | Runtime: `Receipt(envelope_id=..., signature_hash=..., verified_at=..., key_scheme=KeyScheme.BRINE)` raises `TypeError: Receipt.__init__() missing 1 required keyword-only argument: '_token'` (independently confirmed). Static: `uv run mypy --strict tests/fixtures/receipt_misuse.py` exits 1 with `error: Missing named argument "_token" for "Receipt"` (independently confirmed).                                                                          |

**Score:** 3/5 truths verified

### Deferred Items

No items deferred to later phases. The two failing truths block Phase 3 SC3 (cross-role brine round-trip Photophore → pi-forge) and Phase 3 SC1 (the 9-step dispatch flow that exercises `verify-receipt` on real envelope shapes), so they cannot be deferred.

### Required Artifacts

| Artifact                                                          | Expected                                                                  | Status     | Details                                                                                                                                              |
| ----------------------------------------------------------------- | ------------------------------------------------------------------------- | ---------- | ---------------------------------------------------------------------------------------------------------------------------------------------------- |
| `thermocline/python/pyproject.toml`                               | Package metadata, deps pinned, name 'thermocline'                          | ✓ VERIFIED | `pydantic>=2.7,<3.0`, `pynacl>=1.5`, `rfc8785`, `jsonschema>=4.0`, `keyring>=25.0` declared; `requires-python = ">=3.11"`.                          |
| `thermocline/python/src/thermocline/__init__.py`                  | Public API surface re-exports                                             | ✓ VERIFIED | All 22 names in `__all__` (Task, TaskResult, Job, JobResult, ErrorEnvelope, ContentBlock, Sensitive, KeyScheme, SUPPORTED_VERSIONS, validate_version, canonicalize, IdentityProvider, BrineProvider, Verifier, Signature, Receipt, EnvelopeError, UnsupportedVersionError, CanonicalizationError, IdentityError, SchemeError, KeystoreUnavailableError, __version__). |
| `thermocline/python/src/thermocline/version.py`                    | `SUPPORTED_VERSIONS = frozenset({'0.3.0', '0.3.1'})`, `validate_version`  | ✓ VERIFIED | Present, exported.                                                                                                                                  |
| `thermocline/python/src/thermocline/sensitive.py`                  | `Sensitive[T]` with redacting `__repr__`, `.reveal()`, b64 serializer    | ✓ VERIFIED | `Sensitive[bytes]` survives JSON round-trip; `repr` does not leak. (WR-06 / WR-07 noted: `__hash__` and `__eq__` use non-constant-time `bytes` equality — defense-in-depth concern but does not block SC1-5.) |
| `thermocline/python/src/thermocline/envelope.py`                   | Pydantic v2 envelope models with `extra="forbid"`, `Sensitive[bytes]`     | ✓ VERIFIED | Five envelope shapes present; `ConfigDict(extra="forbid")` on each. WR-02 noted: `ContentBlock` does not enforce the tier/content/shadow privacy invariant — does not block SC1-5 but is a defense-in-depth gap. |
| `thermocline/python/src/thermocline/canonical.py`                  | `canonicalize(payload) -> bytes` via `rfc8785`                            | ✓ VERIFIED | Single function, raises `CanonicalizationError`; Hypothesis properties green.                                                                       |
| `thermocline/python/src/thermocline/identity.py`                   | `IdentityProvider` Protocol, `Signature`, `Receipt` (D-01 sentinel), `Verifier`, `BrineProvider` | ⚠️ ORPHANED (effective) | All five symbols present. `BrineProvider` is wired into the public `__all__` and its sign-side path is correct. Verify-side path (BL-02) AND cross-role public-key path (BL-01) are broken — file exists, but the wiring to the documented contract is incomplete. |
| `thermocline/python/scripts/build_schemas.py`                      | Schema generator with `--check` and `--write`                              | ✓ VERIFIED | `python -m thermocline.scripts.build_schemas --check` exits 0; documented in plan.                                                                  |
| `thermocline/schema/{task,task_result,job,job_result,error}.schema.json` | Draft 2020-12 schemas with `$id`                                       | ✓ VERIFIED | All five files present; valid Task fixture validates; build-script drift check is clean.                                                            |
| `thermocline/python/tests/fixtures/receipt_misuse.py`              | mypy `--strict` rejects this file                                          | ✓ VERIFIED | `mypy --strict` exits 1 with `[call-arg]` error on the deliberate misuse line.                                                                       |
| `thermocline/conformance/{valid,invalid}/MANIFEST.yaml`            | Three-level YAML manifest                                                  | ✓ VERIFIED | All three manifests parse; `EXPECTED_AT_C_SURFACES = {AT-C1..AT-C6}` covered.                                                                       |
| `thermocline/conformance/invalid/AT-C1..AT-C6.json`                | Six AT-C surface fixtures                                                  | ✓ VERIFIED | All six files present and parse; AT-C4 / AT-C5 / AT-C6 supposedly Phase-1-wired, but AT-C4 is **structurally only** — no test loads the actual AT-C4 JSON and runs it through `Verifier.verify` (this is itself an artefact of BL-02). |
| `thermocline/CHANGELOG.md`                                          | v0.3.1 patches recorded                                                    | ✓ VERIFIED | Present, includes 0.3.1 entry.                                                                                                                       |
| `thermocline/python/src/thermocline/scripts/check_no_json_dumps.py` | AST lint, exits 0 on clean tree                                           | ✓ VERIFIED | `python -m thermocline.scripts.check_no_json_dumps` exits 0.                                                                                        |

### Key Link Verification

| From                                              | To                                                  | Via                                                                                                                                          | Status     | Details                                                                                                                                       |
| ------------------------------------------------- | --------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------- | ---------- | --------------------------------------------------------------------------------------------------------------------------------------------- |
| `envelope.py`                                     | `sensitive.py`                                       | `ContentBlock.content: Sensitive[bytes] \| None`                                                                                              | ✓ WIRED    | Type annotation present; round-trip Hypothesis test passes.                                                                                  |
| `envelope.py`                                     | `version.py`                                         | Field validator imports `SUPPORTED_VERSIONS` and rejects unknown versions with `UnsupportedVersionError(code=UNSUPPORTED_VERSION)`           | ✓ WIRED    | AT-C5 fixture exercises this path successfully.                                                                                              |
| `scripts/build_schemas.py`                         | `envelope.py`                                        | `model.model_json_schema()` per envelope class, written to `thermocline/schema/<name>.schema.json`                                            | ✓ WIRED    | `--check` exits 0; schema drift gate clean.                                                                                                   |
| `__init__.py`                                     | `envelope.py`                                        | `from .envelope import Task, TaskResult, Job, JobResult, ErrorEnvelope, ContentBlock`                                                        | ✓ WIRED    | All six names exported.                                                                                                                       |
| `canonical.py`                                    | `rfc8785` (third-party)                              | `import rfc8785; rfc8785.dumps(payload)` returns canonical bytes per RFC 8785                                                                 | ✓ WIRED    | Hypothesis round-trip + tamper-detection tests pass.                                                                                         |
| `__init__.py`                                     | `canonical.py`                                       | `from .canonical import canonicalize`                                                                                                        | ✓ WIRED    | Public API exposes `canonicalize` and `CanonicalizationError`.                                                                               |
| `identity.py` `BrineProvider.sign` / `.verify`    | `canonical.py` `canonicalize`                        | `canonicalize(envelope)` is signing input                                                                                                    | ✓ WIRED    | Test `test_brine_signs_canonical_json_input` confirms `canonicalize` is called from `sign`. AST lint forbids `json.dumps` in this module.   |
| `identity.py` `BrineProvider`                     | `python-keyring`                                     | `keyring.get_keyring()` at startup; raises `KeystoreUnavailableError` on `NoKeyringError`; `keyring.get_password(service, identity)` per signature | ⚠️ PARTIAL | `keyring.get_keyring()` is called and `NoKeyringError` is caught. **However**, the substring-on-class-name check fails to reject the real `keyring.backends.fail.Keyring` and `…null.Keyring` (BL-03), so the IDENT-05 contract is not enforced in production. |
| `identity.py` `BrineProvider.verify`              | `BrineProvider.public_key`                            | Verifier looks up the public key from the keystore                                                                                          | ✗ NOT_WIRED CORRECTLY | `public_key()` reads the **private seed** from the keystore and derives the verify key. There is no separate public-key store, so no node can verify another node's signatures without holding that node's seed (BL-01 — fundamental violation of the IdentityProvider Constraint "the public key is the node identity — share it freely"). |
| `Verifier.verify`                                 | envelope `key_scheme` field                          | Reads declared scheme to enforce match against `signature.scheme`                                                                            | ✗ NOT_WIRED CORRECTLY | Reads top-level `envelope.get('key_scheme')`, but real envelopes nest the field under `dispatch_signature.key_scheme` / `receipt_signature.key_scheme`. Independent reproduction with `conformance/valid/task-pi-100-digits.json`: `SchemeError("declared key scheme None does not match signature scheme 'brine'")` (BL-02). |
| `__init__.py`                                     | `identity.py`                                        | `from .identity import BrineProvider, IdentityProvider, Receipt, Signature, Verifier`                                                        | ✓ WIRED    | All five names exported.                                                                                                                      |

### Data-Flow Trace (Level 4)

| Artifact                                    | Data Variable                                              | Source                                                            | Produces Real Data                                                                                                                                                                                          | Status                |
| ------------------------------------------- | ---------------------------------------------------------- | ------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------- |
| `BrineProvider.sign`                        | `seed_hex` from keystore                                    | `keyring.get_password(self._keyring_service, signer_identity)`     | Yes — signs canonical bytes via PyNaCl `SigningKey(...).sign(canonical)`. Independently signed and verified an envelope via in-memory keyring.                                                              | ✓ FLOWING             |
| `BrineProvider.verify`                      | `verify_key_bytes`                                          | `self.public_key(identity=signature.signer_identity)` → reads seed and derives verify key | Yes — IF the verifier's keystore holds the signer's seed. Cross-role flow (verifier holds only public key) is **disconnected** because `public_key()` requires the seed (BL-01).                            | ⚠️ HOLLOW (cross-role) |
| `Verifier.verify`                            | `declared` (envelope key_scheme)                            | `envelope.get('key_scheme')`                                       | No — for real Task / TaskResult / Job / JobResult envelopes, `declared` is always `None`. The dispatch path is **disconnected** from real envelope shapes.                                                  | ✗ DISCONNECTED         |
| `Task.parse_strict` → `UnsupportedVersionError` | rejects unknown versions                                    | `validate_version()` reads `SUPPORTED_VERSIONS`                    | Yes — AT-C5 fixture triggers correct error code.                                                                                                                                                            | ✓ FLOWING             |
| Schema artifacts                             | generated bytes                                              | `model.model_json_schema()`                                         | Yes — five schemas committed; valid Task fixture validates.                                                                                                                                                  | ✓ FLOWING             |

### Behavioral Spot-Checks

| Behavior                                                                                     | Command                                                                                                                       | Result                                                                                                              | Status   |
| -------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------- | -------- |
| Test suite passes                                                                            | `cd thermocline/python && uv run python -m pytest -q`                                                                          | `117 passed in 3.28s`                                                                                                | ✓ PASS    |
| Public API importable                                                                         | `uv run python -c "from thermocline import …; print(thermocline.__all__)"`                                                    | All 22 names import; `__version__` = `0.3.1`.                                                                       | ✓ PASS    |
| `pip install -e` on fresh Python 3.14 venv                                                    | `python3.14 -m venv /tmp/test-thermo-install; /tmp/test-thermo-install/bin/pip install -e thermocline/python`                  | Install OK; full public API importable.                                                                              | ✓ PASS    |
| Schema drift gate clean                                                                       | `uv run python -m thermocline.scripts.build_schemas --check`                                                                  | exit 0                                                                                                              | ✓ PASS    |
| `json.dumps` lint clean                                                                       | `uv run python -m thermocline.scripts.check_no_json_dumps`                                                                    | `ok: no json.dumps found in library code outside allowlist.` (exit 0)                                                | ✓ PASS    |
| Direct `Receipt(...)` raises `TypeError`                                                       | `Receipt(envelope_id=..., signature_hash=..., verified_at=..., key_scheme=KeyScheme.BRINE)` from external scope               | `TypeError: Receipt.__init__() missing 1 required keyword-only argument: '_token'`                                  | ✓ PASS    |
| `mypy --strict` rejects misuse fixture                                                         | `uv run mypy --strict tests/fixtures/receipt_misuse.py`                                                                       | exit 1, `[call-arg]` error on missing `_token`                                                                       | ✓ PASS    |
| `Verifier.verify` accepts a real Task envelope                                                | Load `conformance/valid/task-pi-100-digits.json`, set `dispatch_signature.key_scheme = 'brine'`, pass through `Verifier.verify` | `SchemeError("declared key scheme None does not match signature scheme 'brine'")` — confirms BL-02                  | ✗ FAIL    |
| Real fail/null keyring backend rejected by `BrineProvider.__init__`                            | `from keyring.backends import fail, null; print(fail.Keyring.__name__, null.Keyring.__name__)`                                | Both produce `'Keyring'` — confirms BL-03 — substring `'fail'` / `'null'` not in `'Keyring'`                          | ✗ FAIL    |
| Verifier-only role (no seed in keystore) verifies a foreign signature                          | `BrineProvider.public_key(identity='alice')` after deleting `('thermocline.test', 'alice')` from the in-memory keystore       | `IdentityError: no brine key stored for identity 'alice'` (code IDENTITY_NOT_FOUND) — confirms BL-01                  | ✗ FAIL    |
| `BrineProvider.generate` is idempotent / refuses to clobber                                    | Call `provider.generate(identity='alice')` twice, compare stored seed                                                          | Two distinct seeds stored, no warning — confirms BL-04 (data-loss risk)                                              | ✗ FAIL    |

### Requirements Coverage

| Requirement | Source Plan      | Description                                                                                                                                                                  | Status         | Evidence                                                                                                                                                                                                                                                              |
| ----------- | ---------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| THERMO-01   | 01-03            | Spec patch: cirdan→thermocline rename; v0.3.1 patches recorded in CHANGELOG                                                                                                  | ✓ SATISFIED    | `thermocline/CHANGELOG.md` exists with 0.3.1 entry.                                                                                                                                                                                                                  |
| THERMO-02   | 01-01            | JSON Schema artifacts (Draft 2020-12) under `thermocline/schema/` for `task`, `task_result`, `job`, `job_result`, `error`                                                    | ✓ SATISFIED    | All 5 schema files present; `--check` clean.                                                                                                                                                                                                                          |
| THERMO-03   | 01-01            | Pydantic v2 models with strict validation (no extras / no missing required / type-checked at boundary)                                                                       | ✓ SATISFIED    | `ConfigDict(extra="forbid")` on each shape; AT-C6 negative path verified.                                                                                                                                                                                             |
| THERMO-04   | 01-02            | Single canonical-JSON path `thermocline.canonical.canonicalize` implementing RFC 8785 / JCS                                                                                  | ✓ SATISFIED    | `canonicalize` exposed; AST lint forbids `json.dumps`; Hypothesis property tests assert round-trip stability and tamper detection.                                                                                                                                  |
| THERMO-05   | 01-01            | `pyproject.toml` publishable as `thermocline-py`; pinned to Pydantic v2; supports Python 3.11+                                                                               | ⚠️ NEEDS HUMAN | `pyproject.toml` declares `name = "thermocline"` (not `"thermocline-py"`); `requires-python = ">=3.11"`; pydantic pinned `>=2.7,<3.0`. Per CLAUDE.md note "Thermocline's planned `thermocline-py` registry entry", PyPI publishability under that name should be confirmed by the maintainer. |
| THERMO-06   | 01-03            | Cross-language conformance fixtures under `thermocline/conformance/{valid,invalid}/`, each invalid fixture annotated with AT-* surface                                       | ✓ SATISFIED    | Three-level manifest present; six AT-C fixtures committed.                                                                                                                                                                                                            |
| THERMO-07   | 01-01            | `SUPPORTED_VERSIONS` includes `"0.3.1"` and rejects others with `UNSUPPORTED_VERSION`                                                                                          | ✓ SATISFIED    | AT-C5 fixture (`thermocline=0.0.1`) raises `UnsupportedVersionError(code="UNSUPPORTED_VERSION")`.                                                                                                                                                                      |
| IDENT-01    | 01-03            | `IdentityProvider` exposed as `typing.Protocol` with members `scheme`, `sign`, `verify`, `public_key`, `generate`                                                            | ✓ SATISFIED    | `@runtime_checkable Protocol` with all five members; `isinstance(BrineProvider(...), IdentityProvider)` succeeds in tests.                                                                                                                                            |
| IDENT-02    | 01-03            | Reference adapter for `brine` using PyNaCl + `python-keyring`; never returns the private key, never copies key material out of the keystore, never holds key bytes in process memory beyond what `python-keyring` exposes during the keystore RPC | ✗ BLOCKED      | The adapter signs/verifies via PyNaCl Ed25519 (positive path verified) and never exposes a key-leaking method by name (reflection test passes). **However**, BL-01 means the verify path requires the verifier's keystore to hold the signer's private seed — a structural violation of "private key MUST never leave the secure keystore" once the architecture spans multiple nodes. |
| IDENT-03    | 01-03            | Verifier dispatches on declared `key_scheme`; refuses scheme mismatch                                                                                                        | ✗ BLOCKED      | Dispatch logic exists, but the field-lookup path (BL-02) reads from the wrong dict location for real envelopes — every legitimate signed envelope is rejected with `SchemeError`.                                                                                     |
| IDENT-04    | 01-03            | `Receipt` constructible only via `IdentityProvider.verify` returning success — no public constructor                                                                          | ✓ SATISFIED    | Runtime gate (TypeError) and static gate (`mypy --strict` failure) both verified independently. WR-01 is a defense-in-depth note: any code in the same package can in principle import `_RECEIPT_TOKEN` — the docstring slightly overstates this; does not block IDENT-04. |
| IDENT-05    | 01-03            | Adapter refuses to start when secure keystore is unavailable; no fallback to file or env-var                                                                                 | ✗ BLOCKED      | BL-03 — production fail/null backends are NOT detected by the substring check. The contract is enforced for hand-rolled `FailKeyring` / `NullKeyring` test classes only. The static lint that asserts no filesystem/env imports passes — that half holds.            |

No orphaned requirement IDs in REQUIREMENTS.md for Phase 1 (the row `THERMO-01..07 / IDENT-01..05 → Phase 1` matches the plan declarations).

### Anti-Patterns Found

| File                                                        | Line       | Pattern                                                                                                       | Severity   | Impact                                                                                                                                                                                                                                |
| ----------------------------------------------------------- | ---------- | ------------------------------------------------------------------------------------------------------------- | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `thermocline/python/src/thermocline/identity.py`             | 312-323    | `public_key()` reads the **seed** from the keystore and derives the verify key — no separate public-key store | 🛑 Blocker  | Cross-role verification impossible; breaks IDENT-02 architectural intent and Phase 3 SC3.                                                                                                                                              |
| `thermocline/python/src/thermocline/identity.py`             | 209-234    | `Verifier.verify` reads `envelope.get('key_scheme')` from the top level                                        | 🛑 Blocker  | Every real envelope rejected with `SchemeError`; IDENT-03 not actually wired for production envelopes.                                                                                                                                  |
| `thermocline/python/src/thermocline/identity.py`             | 273-294    | Substring check on `type(backend).__name__` for `'fail'` / `'null'`                                            | 🛑 Blocker  | Production `keyring.backends.fail.Keyring` / `…null.Keyring` are class-named `'Keyring'` — the check passes silently. IDENT-05 fails open in headless / no-D-Bus environments.                                                          |
| `thermocline/python/src/thermocline/identity.py`             | 297-310    | `generate()` calls `set_password` unconditionally — silent overwrite of existing seed                          | ⚠️ Warning | Re-running a setup script destroys the prior signing identity; rotation is supposed to be a deliberate, audited operation. (BL-04 — not strictly an SC1-5 gate, but a foreseeable data-loss path.)                                       |
| `thermocline/python/src/thermocline/sensitive.py`            | 65-73      | `__hash__` and `__eq__` use non-constant-time comparison on inner bytes                                        | ⚠️ Warning | `Sensitive[bytes]` is advertised as a privacy primitive; bytes-equality on secret-bearing wrappers leaks via timing. (WR-06 / WR-07.)                                                                                                  |
| `thermocline/python/src/thermocline/envelope.py`             | 69-81      | `ContentBlock` does not enforce `tier=0` rejection or `content` XOR `shadow` invariant                          | ⚠️ Warning | Defense-in-depth gap — the privacy-tier contract is documented but not type-system-enforced. (WR-02.)                                                                                                                                   |
| `thermocline/python/src/thermocline/identity.py`             | various    | `del signing_key` calls advertised as memory hygiene                                                            | ℹ️ Info    | PyNaCl `SigningKey` does not zero memory on `__del__`; reviewers may misread the `del` as a security control. (WR-05.)                                                                                                                  |

### Human Verification Required

None — every identified gap has an automated repro and is grounded in inspectable source code and observable test behaviour.

If the Phase 1 maintainer wishes to accept any of the BLOCKERs as out-of-scope (e.g., "Phase 1 ships the brine adapter as same-node-only; cross-role verification lands in Phase 3 with public-key registration") this would be a deliberate scope reduction inconsistent with the success criteria as written. Recommend re-planning the gap closures rather than overriding.

### Gaps Summary

Three BLOCKERs from the code review (BL-01, BL-02, BL-03) are independently confirmed and prevent Phase 1 from achieving SC2 and SC4 as written. They cluster around the same module (`thermocline/python/src/thermocline/identity.py`) and the same root cause: tests exercised the brine adapter only with synthetic flat-dict envelopes and a single shared in-memory keystore, masking three real production paths.

A fourth issue (BL-04, silent key clobber) is a data-loss risk but does not directly fail any of the five success criteria — surfaced as a Warning in the anti-pattern table, not a gap.

The other ten Phase 1 deliverables (envelope models, canonical-JSON, schema artifacts, conformance fixtures, Receipt sentinel, install path, Hypothesis properties, json.dumps lint, schema drift gate, requirement coverage for THERMO-01..04/06/07 and IDENT-01/04) are fully verified.

The recommended closure path is one focused plan that:

1. Adds a separate public-key store on `BrineProvider` (or splits the brine adapter into signer-side and verifier-side helpers) so a verifier can hold only verify-keys (BL-01 fix).
2. Reads `key_scheme` from the canonical nested location in `Verifier.verify`, with a regression test that runs a real Task envelope (loaded from `thermocline/conformance/valid/task-pi-100-digits.json`) through Verifier.verify and asserts a Receipt is returned (BL-02 fix; this also wires AT-C4 behaviorally instead of structurally).
3. Replaces the substring heuristic in the keystore probe with a direct `isinstance` check against `keyring.backends.fail.Keyring` / `keyring.backends.null.Keyring`, and updates the IDENT-05 tests to use the real classes (BL-03 fix).
4. Optionally addresses BL-04 by guarding `generate()` against pre-existing entries and stubbing a `rotate()` method.

After closure, re-run all 117 existing tests plus the new behavioral regressions and re-verify against the same five success criteria.

---

_Verified: 2026-05-08T08:20:02Z_
_Verifier: Claude (gsd-verifier)_
