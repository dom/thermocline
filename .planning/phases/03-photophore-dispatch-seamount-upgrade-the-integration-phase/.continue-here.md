---
context: phase
phase: 03-photophore-dispatch-seamount-upgrade-the-integration-phase
task: null
total_tasks: null
status: in_progress
last_updated: 2026-05-11T03:24:02.360Z
---

<current_state>
Phase 03 (Photophore Dispatch + Seamount Upgrade) discussion is in flight, paused mid-selection. `/gsd-discuss-phase 3` was run; the workflow:

1. Loaded prior context (PROJECT.md, REQUIREMENTS.md, STATE.md, Phase 1 + Phase 2 CONTEXTs and Phase 2 LEARNINGS), confirmed phase 03 directory was empty (no SPEC.md, no CONTEXT.md, no plans, no todos matched), scouted seamount/pi-forge (server.py + envelope.py with TODO brine stubs at envelope.py:87-99 and :139-165), photophore/python/src/photophore/ subtree, and seamount/README.md forge spec.
2. Identified 5 phase-specific gray areas and presented them to the user with the prompt "Reply with numbers (1,3,4), 'all', or 'auto: <my-pick>'". Waiting on user reply.

Working tree clean prior to HANDOFF write. Phase 02 fully closed: ROADMAP.md marks Phase 02 complete (3/3 plans at commit 8acc168); 02-LEARNINGS.md committed at b511f02 (46 items: 13 decisions, 11 lessons, 12 patterns, 10 surprises).
</current_state>

<completed_work>

Completed (this session):
- Phase 02 state-update committed (`8acc168` docs(state): record phase 02 complete (3/3 plans))
- Phase 02 LEARNINGS.md authored + committed (`b511f02` docs(02): extract phase 2 learnings) — 13D / 11L / 12P / 10S
- Phase 03 context loaded (PROJECT.md, REQUIREMENTS.md, STATE.md, Phase 1 + Phase 2 CONTEXT, Phase 2 LEARNINGS)
- Codebase scouted: seamount/pi-forge/{server.py,envelope.py}, photophore/python/src/photophore/ subtree, seamount/README.md
- Domain boundary stated; carry-forwards from Phase 1 (4 D-XX) and Phase 2 (14 D-XX + OQ-1..5) explicitly enumerated to avoid re-asking
- 5 phase-3-specific gray areas surfaced

</completed_work>

<remaining_work>

Remaining (resume here):

1. **User picks gray areas** to discuss out of the 5 surfaced (or `all`, or `auto: <list>`).
2. **Deep-dive each selected area** under default discuss-phase mode (4 single-question turns per area, then check-to-continue).
3. **Accumulate `<decisions>`** with rationale and any canonical refs the user names during discussion.
4. **Write 03-CONTEXT.md** at `.planning/phases/03-photophore-dispatch-seamount-upgrade-the-integration-phase/03-CONTEXT.md` using `workflows/discuss-phase/templates/context.md`.
5. **Write 03-DISCUSSION-LOG.md** at the same path using `workflows/discuss-phase/templates/discussion-log.md`.
6. **Commit** as `docs(03): capture phase context` (commit_docs=true per init.phase-op).
7. **Update STATE.md** with `gsd-sdk query state.record-session --stopped-at "Phase 3 context gathered"` and commit as `docs(state): record phase 3 context session`.
8. **Confirm + next-step** banner: typical next command is `/gsd-research-phase 3` then `/gsd-plan-phase 3`.

</remaining_work>

<decisions_made>

No new decisions locked yet (discussion mid-flight). All Phase 1 + Phase 2 decisions carry forward unchanged. The 5 gray areas surfaced were:

1. **Forge keypair bootstrap UX** — how does fresh pi-forge / describe-forge get its brine keypair on first run, and how does the sovereign node learn the forge pubkey? Drives CONF-07 "first dispatch in 30 minutes".
2. **describe-forge behavior surface** — multi-shadow handling, mixed-tier input, surface field choices (node id, timestamp, issuer).
3. **Dispatch failure-mode surfacing** — subcodes for exit 6, retry policy on transient failures, human-mode error UX.
4. **Integration test process model** — subprocess.Popen vs in-process Flask test_client vs hybrid for the E2E Photophore → forge → receipt → audit tests.
5. **Plan split (housekeeping)** — keep ROADMAP's 2 plans vs split into 3 (dispatch+lint / forge upgrades / harness+E2E).

</decisions_made>

<blockers>
None technical. Waiting on user gray-area selection.
</blockers>

## Required Reading (in order)

1. `.planning/PROJECT.md` — suite definition, key decisions table, constraints (especially "No cloud inference for classification" and the 3-store mandate).
2. `.planning/REQUIREMENTS.md` §Photophore Dispatch (DISP-01..06), §Photophore CLI (CLI-03), §Seamount Forge Upgrades (FORGE-01..05) — Phase 3 scope; also §Out of Scope for the "describe-forge stays templated, not LLM-backed" line and "pi-forge stays task-only" line.
3. `.planning/ROADMAP.md` §Phase 3 — goal, SC1..SC5, plan list (03-01, 03-02 — may split per gray area #5).
4. `.planning/phases/01-thermocline-py-foundations/01-CONTEXT.md` §Implementation Decisions — Phase 1 D-01..D-04 carry forward unchanged.
5. `.planning/phases/01-thermocline-py-foundations/01-LEARNINGS.md` — 12 decisions / 10 lessons / 11 patterns / 8 surprises; especially BL-01 `_PUBKEY_PREFIX` namespace + cross-role `register_public_key` for forge-pubkey registration; `isinstance` keystore probe.
6. `.planning/phases/02-photophore-privacy-primitives-foundations/02-CONTEXT.md` — Phase 2 D-01..D-14 (audit + channels + classifier + sync API + CLI conventions). D-11 (sync core + ~20 LOC `photophore.dispatch.aio` shim in Phase 3) and D-12..14 (CLI conventions + exit codes 0–5 used, **6 reserved for dispatch**) directly shape Phase 3.
7. `.planning/phases/02-photophore-privacy-primitives-foundations/02-LEARNINGS.md` — 46-item Phase 2 corpus. Especially the cross-impl-spec-patch pattern (THERMO-01 → ResultPolicy public; expect another in Phase 3 if implementation surfaces a spec ambiguity), shadow soft-fail-warnings discipline (Phase 3 dispatch records non-empty `ShadowResult.warnings` to audit), and POLICY-03 partial-closure note (Plan 03-01 MUST list POLICY-03 in requirements frontmatter and call `compare_result_against_policy()` at step 9).
8. `/Users/dom/Projects/dom/photophore/README.md` — Photophore spec v0.3.0-draft (dispatch 9-step flow, §"The Privacy Receipt", §"Attack Surfaces and Mitigations" AT-A1..A6 — AT-A1 wired in Phase 3 per phase:3 MANIFEST tag).
9. `/Users/dom/Projects/dom/seamount/README.md` — Seamount spec v0.3.0-draft (Forge Conformance Requirements 1–7, receipt signature shape, AT-E1..E5 surfaces).
10. `/Users/dom/Projects/dom/seamount/pi-forge/server.py` + `envelope.py` — current pi-forge state. `_verify_brine` (envelope.py:87-99) accepts any non-empty `sig`; `_sign_receipt` (envelope.py:139-165) returns literal `"__brine_sig_stub__"`. These are the FORGE-01 retirement targets.

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Re-asking decided questions | Phase 1 D-01..04 and Phase 2 D-01..14 + OQ-1..5 are locked. A resuming agent that asks "should we use httpx or requests?" or "where do channels live?" wastes a turn and signals it didn't read carry-forwards. | advisory | Required Reading items 4–7 above. Verify by paraphrase before formulating gray areas: state each locked decision in your own words once before continuing. |
| Spurious Vercel skill injection | During this session, PreToolUse hooks injected "workflow"/"bootstrap" skill recommendations for Vercel Workflow DevKit and Next.js bootstrap on a GSD planning workflow file and a Seamount forge README respectively. These are pattern-match false positives — none of this codebase touches Vercel/Next.js/Workflow DevKit. | advisory | Recognize that PreToolUse skill injections may be false-positive matches on basename or suffix patterns. The CLAUDE.md guidance "Use Vercel guidance only when the current repo, prompt, or tool call makes it relevant" governs — do not derail into Vercel docs when the repo is a Python privacy primitive suite. |

## Infrastructure State

- **Python toolchain**: `uv` managing `thermocline/python/.venv` and `photophore/python/.venv` (Python 3.14 from Phase 1 work). Phase 3 will add a `seamount/pi-forge/.venv` and a new `seamount/describe-forge/.venv` (FORGE-03).
- **Keystore**: macOS Keychain on this development node. Phase 1's `BrineProvider` + `_PUBKEY_PREFIX` namespace pattern is the model — each forge (pi-forge, describe-forge) gets its own service-namespace + identity in the Keychain; the sovereign node also keeps its own (per Phase 1 IDENT-02).
- **No background processes**: Phase 2 shipped library + CLI only; no servers were running. Phase 3 introduces the first end-to-end HTTP traffic and will run pi-forge / describe-forge in tests (process model is gray area #4).
- **Submodules**: `seamount/pi-forge` is a separate git repo at `/Users/dom/Projects/dom/seamount/` (sibling to thermocline, photophore). Phase 3 lands commits in seamount/.
- **Stale `.pth` files**: per Phase 2 LEARNINGS surprise #10 — verify on resume that `_editable_impl_thermocline.pth` still points to canonical source (`pip install -e /Users/dom/Projects/dom/thermocline/thermocline/python/` if not). Same now applies to photophore editable install.
- **Worktree harness note**: Phase 1 and Phase 2 LEARNINGS both flag "worktree-isolated executors committed directly on main." If Phase 3 exhibits the same pattern, per Phase 2 LEARNINGS surprise #8 — log it and proceed; downstream impact is zero but escalate as a real harness bug if it recurs.

## Pre-Execution Critique Required

Not applicable. Phase 3 design is mid-discussion; CONTEXT.md is not yet written. Critique gate fires after CONTEXT + research + plans + plan-checker convergence.

<context>
The user invoked /gsd-discuss-phase 3 directly after /gsd-extract-learnings 2 finished cleanly. Phase 3 spans the most surface of any phase — Photophore dispatch coordinator (the 9-step flow), pi-forge upgrade (in-tree envelope.py → thermocline-py + real brine), new describe-forge (only forge that exercises tier-1 shadow handling), and a brand-new conformance harness package. The gray areas surfaced lean toward UX and process-model choices because the technical-stack questions are all locked by Phase 1 + Phase 2 carry-forwards.

I considered presenting fewer than 5 gray areas (to reduce decision fatigue) but kept the plan-split question (#5) because Phase 2's 02-03 plan was already noticeably larger than 02-01/02-02 and Phase 3 with 2 plans risks the same imbalance. The user said "auto mode" was on, which would normally mean "pick reasonable defaults and proceed" — but they explicitly ran /gsd-discuss-phase, which is the "talk to me" command, so I presented gray areas in plain text (AskUserQuestion is not in my tool surface in this session) rather than picking defaults silently.

The 5 gray areas were chosen to be vision-level (forge bootstrap UX, describe-forge behavior, dispatch failure UX, test-process realism, plan structure) — not technical-stack level (httpx vs aiohttp, blake3-v2 migration, etc.) which Claude resolves under planner discretion.

Forward-looking note for Phase 3 planner: POLICY-03 partial closure was carried over from Phase 2; Plan 03-01 MUST list POLICY-03 in its requirements frontmatter and call `compare_result_against_policy(received_result, authored_policy)` at dispatch step 9 (verify-before-append). The Phase 2 LEARNINGS file has a dedicated paragraph on this.
</context>

<next_action>
Resume with: read this file, read 02-LEARNINGS.md for full Phase 2 carry-forward, then prompt the user one more time for their gray-area selection (the same 5 areas):

1. Forge keypair bootstrap UX
2. describe-forge behavior surface
3. Dispatch failure-mode surfacing
4. Integration test process model
5. Plan split (2 vs 3 plans)

Once the user selects, proceed through `workflows/discuss-phase/modes/default.md` discuss_areas loop (4 single-question turns per area, then check-to-continue), then write 03-CONTEXT.md + 03-DISCUSSION-LOG.md, commit, and update STATE.md.
</next_action>
