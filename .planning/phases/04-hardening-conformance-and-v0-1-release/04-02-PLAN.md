---
phase: 04-hardening-conformance-and-v0-1-release
plan: 02
type: execute
wave: 1
depends_on: [04-01]
files_modified:
  # 7 ADRs (5 thermocline, 2 photophore) + 3 ADR indexes
  - thermocline/docs/adr/ADR-0001-python-3-11-as-primary-language.md
  - thermocline/docs/adr/ADR-0002-pydantic-v2-lock-in.md
  - thermocline/docs/adr/ADR-0003-single-canonical-json-path.md
  - thermocline/docs/adr/ADR-0004-blake3-with-algo-version.md
  - thermocline/docs/adr/ADR-0005-no-in-process-key-material.md
  - thermocline/docs/adr/index.md
  - photophore/docs/adr/ADR-0001-trust-store-separation-from-audit-log.md
  - photophore/docs/adr/ADR-0002-no-shadow-caching.md
  - photophore/docs/adr/index.md
  - seamount/docs/adr/index.md
  # SP-3.3-01..03 spec amendments
  - thermocline/README.md
  - thermocline/thermocline/CHANGELOG.md
  # Install + ops docs
  - thermocline/docs/install.md
  - thermocline/docs/ops.md
  - thermocline/docs/quickstart.md
  - thermocline/docs/index.md
  - photophore/docs/install.md
  - photophore/docs/ops.md
  - photophore/docs/index.md
  - seamount/pi-forge/docs/install.md
  - seamount/describe-forge/docs/install.md
  - seamount/conformance/docs/install.md
  # README cross-reference additions
  - photophore/README.md
  - seamount/README.md
  # Release script
  - thermocline/scripts/tag-v0.1.0.sh
  # CHANGELOGs (2 new, 1 modify)
  - photophore/CHANGELOG.md
  - seamount/CHANGELOG.md
  # State + project evolution (post-release housekeeping)
  - .planning/STATE.md
  - .planning/PROJECT.md
  - .planning/ROADMAP.md
autonomous: false
requirements: [CONF-05, CONF-07, CONF-08]

must_haves:
  truths:
    - "7 ADRs exist in MADR-lite format (Context / Decision / Consequences / References + Status line) across thermocline/docs/adr/ (5) and photophore/docs/adr/ (2)."
    - "Each repo's README has an Architecture Decision Records section linking to its own docs/adr/index.md plus relative-path cross-references to inherited thermocline ADRs."
    - "Thermocline README.md has SP-3.3-01..03 normative paragraphs in §'Dispatch Signatures' and §'Receipt Signatures'."
    - "Thermocline CHANGELOG.md has a ## [0.3.1] section dating the existing in-progress entries and noting SP-3.3-01..03 reclassification (CONFLICT-02)."
    - "Each repo has docs/install.md + docs/ops.md (or per-forge install.md for seamount); thermocline has docs/quickstart.md walking clone → first dispatch → audit query → audit export on macOS in under 30 minutes."
    - "thermocline/scripts/tag-v0.1.0.sh exists with --dry-run mode; reads THERMOCLINE_SUITE_ROOT env var; asserts clean working trees, branch=main, remote-up-to-date, pytest pass, CHANGELOG has dated ## [0.1.0] heading; then tags each repo with identical message; reminds operator to git push --tags manually."
    - "Each repo has a CHANGELOG.md with ## [0.1.0] - YYYY-MM-DD section in Keep-a-Changelog-lite format (Added / Implemented / Deferred / Known limitations)."
    - "Three coordinated v0.1.0 git tags exist after the human-action checkpoint completes (one each in thermocline, photophore, seamount)."
  artifacts:
    - path: "thermocline/scripts/tag-v0.1.0.sh"
      provides: "release coordination script with --dry-run"
      contains: "THERMOCLINE_SUITE_ROOT"
    - path: "thermocline/docs/quickstart.md"
      provides: "30-minute cross-repo walkthrough (CONF-07)"
      contains: "PIFORGE_READY"
    - path: "thermocline/docs/adr/ADR-0003-single-canonical-json-path.md"
      provides: "ADR for canonical JSON (CONF-05)"
      contains: "## Decision"
    - path: "photophore/docs/adr/ADR-0001-trust-store-separation-from-audit-log.md"
      provides: "ADR for trust-store separation (CONF-05)"
      contains: "## Decision"
    - path: "photophore/CHANGELOG.md"
      provides: "Keep-a-Changelog-lite for photophore v0.1.0"
      contains: "## [0.1.0]"
    - path: "seamount/CHANGELOG.md"
      provides: "Keep-a-Changelog-lite for seamount v0.1.0"
      contains: "## [0.1.0]"
  key_links:
    - from: "thermocline/scripts/tag-v0.1.0.sh"
      to: "thermocline/tools/at_coverage_total.py"
      via: "release script runs pre-tag lint sweep including AT-* roll-up"
      pattern: "at_coverage_total.py"
    - from: "photophore/README.md"
      to: "thermocline/docs/adr/ADR-0005-no-in-process-key-material.md"
      via: "relative-path cross-reference (no symlinks per D-03)"
      pattern: "../thermocline/docs/adr/"
    - from: "thermocline/README.md (amended)"
      to: "thermocline/thermocline/CHANGELOG.md"
      via: "SP-3.3-01..03 spec amendments documented in CHANGELOG 0.3.1 entry"
      pattern: "## \\[0.3.1\\]"
    - from: "thermocline/docs/quickstart.md"
      to: "seamount/pi-forge/pi_forge/__main__.py"
      via: "quickstart references pi-forge init + PIFORGE_READY marker"
      pattern: "pi_forge init"
---

<objective>
Ship the human-readable + release-coordination deliverables that turn Plan 04-01's machine-verifiable green CI into a real v0.1 release: 7 MADR-lite ADRs distributed across thermocline (5) and photophore (2) with cross-references from each repo's README; SP-3.3-01..03 spec amendments to `thermocline/README.md` accompanied by a dated `## [0.3.1]` CHANGELOG entry that resolves CONFLICT-02 (Phase 3 SUMMARY called these coordinator-internal; CONTEXT D-02 reclassifies as spec patches); per-repo `docs/install.md` + `docs/ops.md` plus the cross-repo `thermocline/docs/quickstart.md` walkthrough (clone → first dispatch → audit query → audit export, target <30 min on macOS); two new `CHANGELOG.md` files (photophore + seamount) and one extension to thermocline's existing one — all in Keep-a-Changelog-lite format; `thermocline/scripts/tag-v0.1.0.sh` release helper with `--dry-run` mode; and finally the human-action checkpoint that executes the script in dry-run-then-real mode and lands three coordinated v0.1.0 git tags. STATE.md + PROJECT.md + ROADMAP.md updates close the milestone.

Purpose: Plan 04-01 proved the suite is correct under CI; Plan 04-02 makes that correctness discoverable (ADRs explain why; docs walk a new user through it; CHANGELOGs declare what's in and what's deferred). Then the release script tags it.

Output: 27 file changes (24 new + 3 modify) across thermocline/photophore/seamount + 3 git tags as the release artifact.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-hardening-conformance-and-v0-1-release/04-CONTEXT.md
@.planning/phases/04-hardening-conformance-and-v0-1-release/04-RESEARCH.md
@.planning/phases/04-hardening-conformance-and-v0-1-release/04-PATTERNS.md
@.planning/phases/04-hardening-conformance-and-v0-1-release/04-01-PLAN.md

# Plan 04-01 SUMMARY (only after 04-01 lands):
# @.planning/phases/04-hardening-conformance-and-v0-1-release/04-01-SUMMARY.md

# Source specs being amended (SP-3.3-01..03):
@/Users/dom/Projects/dom/thermocline/README.md
@/Users/dom/Projects/dom/thermocline/thermocline/CHANGELOG.md

# Existing READMEs receiving ADR cross-refs:
@/Users/dom/Projects/dom/photophore/README.md
@/Users/dom/Projects/dom/seamount/README.md
</context>

<interfaces>
<!-- ADR cross-reference paths (cross-file invariant 6). -->

From photophore/README.md adding §"Architecture Decision Records":
```markdown
## Architecture Decision Records

### Photophore-specific
- [ADR-0001: Trust-store separation from audit log](docs/adr/ADR-0001-trust-store-separation-from-audit-log.md)
- [ADR-0002: No shadow caching](docs/adr/ADR-0002-no-shadow-caching.md)

### Inherited from `thermocline-py`
- [ADR-0001: Python 3.11 as primary language](../thermocline/docs/adr/ADR-0001-python-3-11-as-primary-language.md)
- [ADR-0003: Single canonical JSON path](../thermocline/docs/adr/ADR-0003-single-canonical-json-path.md)
- [ADR-0004: BLAKE3 with `algo_version` chain](../thermocline/docs/adr/ADR-0004-blake3-with-algo-version.md)
- [ADR-0005: No in-process key material](../thermocline/docs/adr/ADR-0005-no-in-process-key-material.md)
```

CHANGELOG-lite template (Keep-a-Changelog-lite per D-06):
```markdown
## [0.1.0] - 2026-MM-DD

### Added
- ...

### Implemented
- ...

### Deferred to subsequent milestones
- ...

### Known limitations
- ...
```

Tag message identical across three repos (cross-file invariant 14):
```
v0.1.0 — coordinated with thermocline v0.1.0 + photophore v0.1.0 + seamount v0.1.0
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 5 thermocline ADRs + ADR index + README ADR section</name>
  <read_first>
    - .planning/phases/04-hardening-conformance-and-v0-1-release/04-RESEARCH.md §"ADR Landings" (lines 316-335 — sources per ADR) and §"Code Examples" Example 3 (lines 1293-1334 — ADR-0003 verbatim) + Example 4 (lines 1336-1352 — README cross-ref shape)
    - .planning/PROJECT.md §"Key Decisions" table (lines ~108-115 — source material for ADR-0001 + ADR-0002)
    - /Users/dom/Projects/dom/thermocline/thermocline/python/src/thermocline/scripts/check_no_json_dumps.py (ADR-0003 references this enforcement)
    - /Users/dom/Projects/dom/photophore/python/src/photophore/audit/_chain.py (ADR-0004 references `ALGO_VERSION_DEFAULT`)
    - /Users/dom/Projects/dom/thermocline/thermocline/CHANGELOG.md (existing entries; BL-03 keystore-required pattern for ADR-0005)
    - /Users/dom/Projects/dom/thermocline/README.md (target for §"Architecture Decision Records" addition)
    - .planning/phases/04-hardening-conformance-and-v0-1-release/04-PATTERNS.md Group H (lines 502-554) and cross-file invariants 7-8 (ADR numbering + MADR-lite section ordering)
  </read_first>
  <behavior>
    - 5 ADR files exist at `thermocline/docs/adr/ADR-000{1..5}-<name>.md`.
    - Each file follows MADR-lite section ordering (cross-file invariant 8): `**Status:** Accepted · 2026-MM-DD` header, then `## Context`, `## Decision`, `## Consequences`, `## References`.
    - `thermocline/docs/adr/index.md` exists with one line per ADR (`[ADR-NNNN: title](file) — Accepted, YYYY-MM-DD`).
    - `thermocline/README.md` has a new §"Architecture Decision Records" section linking to `docs/adr/index.md`.
    - Each ADR is one page max (~80-120 lines max including front matter).
  </behavior>
  <files>
    thermocline/docs/adr/ADR-0001-python-3-11-as-primary-language.md,
    thermocline/docs/adr/ADR-0002-pydantic-v2-lock-in.md,
    thermocline/docs/adr/ADR-0003-single-canonical-json-path.md,
    thermocline/docs/adr/ADR-0004-blake3-with-algo-version.md,
    thermocline/docs/adr/ADR-0005-no-in-process-key-material.md,
    thermocline/docs/adr/index.md,
    thermocline/README.md
  </files>
  <action>
1. **Create `thermocline/docs/adr/ADR-0001-python-3-11-as-primary-language.md`**:
```markdown
# ADR-0001: Python 3.11 as primary language

**Status:** Accepted · 2026-MM-DD

## Context

The Thermocline suite ships three coordinated Python packages (`thermocline-py`,
`photophore`, `pi-forge`, `describe-forge`) plus a cross-suite conformance
harness. Choosing one primary language for the v0.1 reference implementation
reduces cross-impl drift and lets all four packages share a single envelope
type system, single canonical-JSON path, and single IdentityProvider Protocol.

Python 3.11 brings significant performance improvements and stable type-system
features (`Self`, `LiteralString`); python-keyring backs the macOS Keychain,
Linux libsecret, and Windows Credential Manager from one API; PyNaCl provides
audited ed25519; mature stdlib `sqlite3` covers the audit log requirement
without an extra dependency.

## Decision

The v0.1 reference implementation of all three Thermocline-suite specs is
written in Python 3.11+ (`requires-python = ">=3.11"` in every `pyproject.toml`).
Other-language implementations (Rust, TypeScript, Swift) are deferred to
post-v0.1 milestones.

## Consequences

- ✓ Single envelope type system shared across `thermocline-py` and `photophore`.
- ✓ Existing `seamount/pi-forge/` (Python 3.11+, Flask, mpmath) integrates without rewrite.
- ✓ `python-keyring` covers all three target platforms (macOS first-class; Linux/Windows secondary).
- ✗ Deployments needing a single binary must bundle CPython; statically-linked builds are slow.
- ✗ Cross-impl ports remain a post-v0.1 concern; the JSON Schema artifacts in
  `thermocline/schema/` keep the wire-level contract language-agnostic.

## References

- PROJECT.md §"Key Decisions"
- CLAUDE.md project instructions ("Language — Python 3.11+")
- pyproject.toml files in all four packages
```

2. **Create `thermocline/docs/adr/ADR-0002-pydantic-v2-lock-in.md`** — same MADR-lite shape; sources: PROJECT.md line 113 + pyproject.toml `pydantic>=2.7,<3.0` pin. Body:
```markdown
# ADR-0002: Pydantic v2 lock-in

**Status:** Accepted · 2026-MM-DD

## Context

Every envelope type in `thermocline-py` (`Task`, `TaskResult`, `Job`, `JobResult`,
`Error`) and every audit-payload type in `photophore` is a typed model with
strict validation at the boundary (no extra fields, no missing required fields,
type-checked content). Pydantic v2's Rust-backed core delivers the validation
performance the suite needs while generating JSON Schema artifacts for free
(used in `thermocline/schema/`).

Pydantic v1 and v2 are not API-compatible in important ways (validator decorators,
config keys, `model_dump`/`model_validate` rename). Mixing versions across
sub-packages would multiply maintenance.

## Decision

All Pydantic dependencies in the suite are pinned to `pydantic>=2.7,<3.0`.
v0.1 will not support v1 callers; envelope models use v2 features
(`model_config = ConfigDict(extra="forbid")`, `Annotated[...]` validators,
`field_validator` rather than `validator`).

## Consequences

- ✓ Single rust-backed validation core; one validation pattern across packages.
- ✓ JSON Schema 2020-12 generation matches what's in `thermocline/schema/`.
- ✓ THERMO-05 (`pyproject.toml` Pydantic v2 pin) is enforced.
- ✗ Downstream applications still on Pydantic v1 cannot consume `thermocline-py` directly.
- ✗ Major version bumps to Pydantic v3 will require a coordinated suite migration.

## References

- THERMO-03, THERMO-05 in REQUIREMENTS.md
- pyproject.toml `pydantic>=2.7,<3.0` pin
- Pydantic v2 release notes (https://docs.pydantic.dev/)
```

3. **Create `thermocline/docs/adr/ADR-0003-single-canonical-json-path.md`** — copy VERBATIM from RESEARCH §"Code Examples" Example 3 (lines 1294-1334). Set Status date to actual day.

4. **Create `thermocline/docs/adr/ADR-0004-blake3-with-algo-version.md`**:
```markdown
# ADR-0004: BLAKE3 with `algo_version` chain

**Status:** Accepted · 2026-MM-DD

## Context

The Photophore audit log is the proof artifact for every channel transition,
every dispatch, and (Phase 4 onward) every CLI invocation. The log MUST be
append-only and cryptographically chained so that any single-byte tamper
invalidates all subsequent entries — this is what AT-A6 protects against.

BLAKE3 is significantly faster than SHA-256, modern, and audited. Its
parallelism + tree-mode features matter less than its simplicity here, but
the speed margin makes per-entry chaining cheap. A versioned `algo_version`
field (e.g., `"blake3-v1"`) keeps the door open for a future migration without
breaking the append-only invariant of existing chains.

## Decision

The Photophore audit chain uses BLAKE3 hashing with an explicit `algo_version`
field on every entry. v0.1 ships `algo_version="blake3-v1"`. The chain's
verifier dispatches on `algo_version`; entries with unknown versions fail
verification with a structured error. Plan 04-01's `_assert_no_sensitive`
runtime guard ensures audit payloads never carry privacy-sensitive content
(defense in depth on top of `Sensitive[T]` static typing).

## Consequences

- ✓ Chain verification scales: BLAKE3 hashes a 1KB payload in microseconds.
- ✓ `algo_version` field provides a clean migration path for future hash families.
- ✓ Same hash family used for CLI arg sanitization (D-07) — `blake3:<hex>` for
  file-path args records correlation without leaking content.
- ✗ Requires the `blake3` Python wheel; pure-stdlib alternatives (SHA-256) are
  the documented fallback if no wheel exists for a target platform.

## References

- AUDIT-02 in REQUIREMENTS.md
- `photophore/python/src/photophore/audit/_chain.py` `ALGO_VERSION_DEFAULT`
- BLAKE3 spec: https://github.com/BLAKE3-team/BLAKE3
```

5. **Create `thermocline/docs/adr/ADR-0005-no-in-process-key-material.md`**:
```markdown
# ADR-0005: No in-process key material

**Status:** Accepted · 2026-MM-DD

## Context

The Thermocline threat model treats node identity (an ed25519 keypair) as the
single most valuable secret in the system. Compromise of a private key permits
forge impersonation (AT-E4), forged dispatch signatures (AT-C4), and channel
impersonation. The mitigation is: the private key MUST never enter the
implementing process's address space outside the brief RPC window the platform
keystore necessarily exposes.

`python-keyring` provides this property on macOS (Keychain), Linux (libsecret),
and Windows (Credential Manager). The reference brine adapter calls the keystore
for each signature operation and discards the returned bytes immediately.

## Decision

`thermocline.IdentityProvider` is a Protocol with `sign` and `verify` methods
that return `Signature` and `Receipt` value types, NOT `PrivateKey`. The brine
reference adapter holds no key material between RPCs; the adapter refuses to
start when `python-keyring` cannot reach the platform secure keystore (no
fall-back to file or env-var storage). `Receipt` is constructible only by
`IdentityProvider.verify` returning success — "skipped verification" cannot
be expressed in code (IDENT-04 / Phase 1 BL-03).

## Consequences

- ✓ Key material never crosses the process boundary except through the keystore RPC.
- ✓ Hardware-backed keystores (Apple Silicon Secure Enclave, Windows TPM) are
  drop-in upgrades — no code changes needed.
- ✓ Tests cannot "skip verification" — type system forbids it.
- ✗ Performance: each signature is a keystore RPC. For high-throughput dispatch
  this could add latency; v0.1 dispatch volume makes this acceptable.
- ✗ macOS Secure Enclave hardware-anchored entries require a developer signing
  identity; deferred to v0.2 per CHANGELOG known-limitations.

## References

- IDENT-02, IDENT-04, IDENT-05 in REQUIREMENTS.md
- CLAUDE.md "Tech — keys" entry
- Phase 1 LEARNINGS BL-03 (keystore-required guard)
- `thermocline/python/src/thermocline/identity.py`
```

6. **Create `thermocline/docs/adr/index.md`**:
```markdown
# Architecture Decision Records (Thermocline)

Forever-decisions that bind the `thermocline-py` reference implementation.
Each ADR is one page max in MADR-lite format (Context / Decision / Consequences / References).

- [ADR-0001: Python 3.11 as primary language](ADR-0001-python-3-11-as-primary-language.md) — Accepted, 2026-MM-DD
- [ADR-0002: Pydantic v2 lock-in](ADR-0002-pydantic-v2-lock-in.md) — Accepted, 2026-MM-DD
- [ADR-0003: Single canonical JSON path](ADR-0003-single-canonical-json-path.md) — Accepted, 2026-MM-DD
- [ADR-0004: BLAKE3 with `algo_version` chain](ADR-0004-blake3-with-algo-version.md) — Accepted, 2026-MM-DD
- [ADR-0005: No in-process key material](ADR-0005-no-in-process-key-material.md) — Accepted, 2026-MM-DD

See also: [photophore ADRs](../../../photophore/docs/adr/index.md) (uses ADR-0003 + ADR-0005).
```

7. **Edit `thermocline/README.md`** — add a new section near the end (before any §"License" section):
```markdown
## Architecture Decision Records

Forever-decisions of the `thermocline-py` reference implementation are recorded
as ADRs under [docs/adr/](docs/adr/index.md). See:

- [ADR-0001: Python 3.11 as primary language](docs/adr/ADR-0001-python-3-11-as-primary-language.md)
- [ADR-0002: Pydantic v2 lock-in](docs/adr/ADR-0002-pydantic-v2-lock-in.md)
- [ADR-0003: Single canonical JSON path](docs/adr/ADR-0003-single-canonical-json-path.md)
- [ADR-0004: BLAKE3 with `algo_version` chain](docs/adr/ADR-0004-blake3-with-algo-version.md)
- [ADR-0005: No in-process key material](docs/adr/ADR-0005-no-in-process-key-material.md)

Photophore and Seamount cross-reference these ADRs from their own READMEs.
```

8. **Replace `2026-MM-DD` with today's UTC date** in all 6 created files (Status lines + index dates). Use `date -u +%Y-%m-%d`.
  </action>
  <verify>
    <automated>
      for f in /Users/dom/Projects/dom/thermocline/docs/adr/ADR-000{1..5}-*.md; do \
        test -f "$f" && grep -q "## Decision" "$f" && grep -q "## Context" "$f" && grep -q "## Consequences" "$f" || exit 1; \
      done && \
      grep -q "Architecture Decision Records" /Users/dom/Projects/dom/thermocline/README.md && \
      grep -q "ADR-0003" /Users/dom/Projects/dom/thermocline/docs/adr/index.md && \
      ! grep -q "2026-MM-DD" /Users/dom/Projects/dom/thermocline/docs/adr/ADR-0001-python-3-11-as-primary-language.md
    </automated>
  </verify>
  <done>
    - 5 ADR files exist with MADR-lite section ordering.
    - `docs/adr/index.md` exists with 5 ADRs listed.
    - `thermocline/README.md` has §"Architecture Decision Records" section.
    - All Status dates are actual ISO dates (no `MM-DD` placeholders remain).
  </done>
</task>

<task type="auto">
  <name>Task 2: 2 photophore ADRs + photophore + seamount README cross-refs + ADR indexes</name>
  <read_first>
    - .planning/phases/04-hardening-conformance-and-v0-1-release/04-RESEARCH.md §"ADR Landings" (rows for photophore ADRs)
    - /Users/dom/Projects/dom/photophore/python/tests/test_channels_separation.py (test that validates the three-store rule referenced by ADR-0001)
    - /Users/dom/Projects/dom/photophore/python/tests/test_shadow_no_caching.py (test referenced by ADR-0002)
    - /Users/dom/Projects/dom/photophore/python/src/photophore/audit/ + /Users/dom/Projects/dom/photophore/python/src/photophore/channels/ (modules implementing the separation)
    - /Users/dom/Projects/dom/photophore/python/src/photophore/shadow/ (no-caching enforcement)
    - /Users/dom/Projects/dom/photophore/README.md (target for §"Architecture Decision Records")
    - /Users/dom/Projects/dom/seamount/README.md (target for cross-ref to thermocline ADRs; no own ADRs per D-03)
    - .planning/phases/04-hardening-conformance-and-v0-1-release/04-PATTERNS.md Group H (lines 529-553) and Group J (lines 610-631)
  </read_first>
  <behavior>
    - 2 photophore ADR files exist at `photophore/docs/adr/ADR-000{1,2}-<name>.md` in MADR-lite format.
    - `photophore/docs/adr/index.md` lists both with status + date.
    - `photophore/README.md` has a new §"Architecture Decision Records" section with both own ADRs + 4 inherited cross-refs (relative paths to thermocline).
    - `seamount/docs/adr/index.md` exists with cross-refs only (no own ADRs per D-03 — links to thermocline ADR-0001, ADR-0003, ADR-0005).
    - `seamount/README.md` has new §"Architecture Decision Records" section linking to its own (cross-ref-only) index + relative paths to relevant thermocline ADRs.
  </behavior>
  <files>
    photophore/docs/adr/ADR-0001-trust-store-separation-from-audit-log.md,
    photophore/docs/adr/ADR-0002-no-shadow-caching.md,
    photophore/docs/adr/index.md,
    photophore/README.md,
    seamount/docs/adr/index.md,
    seamount/README.md
  </files>
  <action>
1. **Create `photophore/docs/adr/ADR-0001-trust-store-separation-from-audit-log.md`**:
```markdown
# ADR-0001: Trust-store separation from audit log

**Status:** Accepted · 2026-MM-DD

## Context

The Photophore trust store records channel state (remote node identity, trust
ceiling, key scheme). The audit log records every operation that produced or
modified that state. If both were backed by the same storage, a tamper of the
audit log could silently rewrite history without detection by the trust store
(and vice versa). The third store — the IdentityProvider — must also be
independent so that key material lives nowhere except the platform secure
keystore.

This is the three-store model: each store has its own backing technology, its
own access pattern, and its own threat model. CHAN-04 mandates separation as
a v0.1 requirement; AT-A5 specifically tests for co-location.

## Decision

The Photophore trust store is backed by `python-keyring` (Keychain / libsecret /
Credential Manager). The audit log is backed by SQLite with append-only
triggers. The IdentityProvider's private key material lives in `python-keyring`
under a separate service namespace (`thermocline.brine`, `seamount.piforge`,
etc.). The three stores share no backing technology and no namespace.

## Consequences

- ✓ Tamper of any one store leaves evidence in the other two.
- ✓ `python-keyring` and SQLite have different operational characteristics
  (different threat models); a single attack class rarely defeats both.
- ✓ Test `test_channels_separation.py` enforces the no-mixing rule at CI time.
- ✗ Three stores = three points of operational concern (backup, migration, recovery).
- ✗ Cross-store consistency is application-layer (atomic three-step pattern from Phase 2 D-07).

## References

- CHAN-04 in REQUIREMENTS.md
- Phase 2 D-04 three-store model
- `photophore/python/tests/test_channels_separation.py`
- [AT-A5 negative test](../../python/tests/at_negative/test_at_a5_trust_store_tampering.py)
```

2. **Create `photophore/docs/adr/ADR-0002-no-shadow-caching.md`**:
```markdown
# ADR-0002: No shadow caching

**Status:** Accepted · 2026-MM-DD

## Context

Shadows are tier-1 abstractions generated at dispatch time. Their identifiers
(`shadow_id`) MUST be unique per dispatch — if they were not, an observer of
the wire traffic could correlate multiple dispatches of the same source content,
defeating the privacy tier. SHADOW-06 mandates the uniqueness invariant.

Caching shadows would be a tempting performance optimization (regenerating the
same abstraction string is wasteful) but would create exactly the correlation
risk the tier exists to prevent. AT-A2 and AT-C3 specifically test this.

## Decision

`photophore.shadow.generate()` produces a fresh `shadow_id` via
`secrets.token_bytes` (UUIDv4 over `os.urandom`) on every call. No caching layer
exists at any level (function, module, process, IPC). The classifier_default
property test plus the dispatch-integrated shadow uniqueness property test
(Plan 04-01 Task 2) prove the invariant under N=200 generation cycles.

## Consequences

- ✓ AT-A2 (shadow inference via shadow_id correlation) is structurally prevented.
- ✓ The abstraction string itself MAY repeat (acceptable — it's the visible
  abstraction); only the `shadow_id` MUST differ.
- ✗ Regenerating identical abstractions wastes minor CPU (acceptable cost for
  the privacy guarantee).
- ✗ Distributed coordination is harder: shadow generators in two processes
  must coordinate on uniqueness only via `os.urandom` collision probability
  (acceptable — birthday-paradox math gives 50% collision at ~2^64 shadows).

## References

- SHADOW-06 in REQUIREMENTS.md
- `photophore/python/tests/test_shadow_no_caching.py`
- `photophore/python/tests/test_shadow_uniqueness_property.py` (CONF-03 #4)
- [AT-A2 negative test](../../python/tests/at_negative/test_at_a2_shadow_correlation.py)
```

3. **Create `photophore/docs/adr/index.md`**:
```markdown
# Architecture Decision Records (Photophore)

Photophore-specific forever-decisions. Cross-reference [thermocline ADRs](../../../thermocline/docs/adr/index.md) for suite-wide decisions.

## Photophore-specific
- [ADR-0001: Trust-store separation from audit log](ADR-0001-trust-store-separation-from-audit-log.md) — Accepted, 2026-MM-DD
- [ADR-0002: No shadow caching](ADR-0002-no-shadow-caching.md) — Accepted, 2026-MM-DD

## Inherited from `thermocline-py`
- [ADR-0001: Python 3.11 as primary language](../../../thermocline/docs/adr/ADR-0001-python-3-11-as-primary-language.md)
- [ADR-0003: Single canonical JSON path](../../../thermocline/docs/adr/ADR-0003-single-canonical-json-path.md)
- [ADR-0004: BLAKE3 with `algo_version` chain](../../../thermocline/docs/adr/ADR-0004-blake3-with-algo-version.md)
- [ADR-0005: No in-process key material](../../../thermocline/docs/adr/ADR-0005-no-in-process-key-material.md)
```

4. **Edit `photophore/README.md`** — append section before any §"License":
```markdown
## Architecture Decision Records

### Photophore-specific
- [ADR-0001: Trust-store separation from audit log](docs/adr/ADR-0001-trust-store-separation-from-audit-log.md)
- [ADR-0002: No shadow caching](docs/adr/ADR-0002-no-shadow-caching.md)

### Inherited from `thermocline-py`
- [ADR-0001: Python 3.11 as primary language](../thermocline/docs/adr/ADR-0001-python-3-11-as-primary-language.md)
- [ADR-0003: Single canonical JSON path](../thermocline/docs/adr/ADR-0003-single-canonical-json-path.md)
- [ADR-0004: BLAKE3 with `algo_version` chain](../thermocline/docs/adr/ADR-0004-blake3-with-algo-version.md)
- [ADR-0005: No in-process key material](../thermocline/docs/adr/ADR-0005-no-in-process-key-material.md)

See [docs/adr/index.md](docs/adr/index.md) for status + dates.
```

5. **Create `seamount/docs/adr/index.md`** (cross-refs only):
```markdown
# Architecture Decision Records (Seamount)

Seamount inherits suite-wide architecture decisions from `thermocline-py`.
Seamount has no repo-specific ADRs in v0.1 (per D-03 in Phase 4 CONTEXT).

## Inherited from `thermocline-py`
- [ADR-0001: Python 3.11 as primary language](../../../thermocline/docs/adr/ADR-0001-python-3-11-as-primary-language.md)
- [ADR-0003: Single canonical JSON path](../../../thermocline/docs/adr/ADR-0003-single-canonical-json-path.md)
- [ADR-0005: No in-process key material](../../../thermocline/docs/adr/ADR-0005-no-in-process-key-material.md)

Seamount-specific concerns (forge isolation, conformance harness scope) are
documented in `seamount/README.md` §"Forge Conformance Requirements" and
do not warrant their own ADRs at the v0.1 maturity level.
```

6. **Edit `seamount/README.md`** — append section:
```markdown
## Architecture Decision Records

Seamount inherits suite-wide ADRs from `thermocline-py`:

- [ADR-0001: Python 3.11 as primary language](../thermocline/docs/adr/ADR-0001-python-3-11-as-primary-language.md)
- [ADR-0003: Single canonical JSON path](../thermocline/docs/adr/ADR-0003-single-canonical-json-path.md)
- [ADR-0005: No in-process key material](../thermocline/docs/adr/ADR-0005-no-in-process-key-material.md)

See [docs/adr/index.md](docs/adr/index.md) for the full index.
```

7. **Replace `2026-MM-DD`** with today's UTC date in all created files.
  </action>
  <verify>
    <automated>
      for f in /Users/dom/Projects/dom/photophore/docs/adr/ADR-000{1,2}-*.md; do \
        test -f "$f" && grep -q "## Decision" "$f" || exit 1; \
      done && \
      grep -q "Architecture Decision Records" /Users/dom/Projects/dom/photophore/README.md && \
      grep -q "../thermocline/docs/adr/ADR-0005" /Users/dom/Projects/dom/photophore/README.md && \
      grep -q "Architecture Decision Records" /Users/dom/Projects/dom/seamount/README.md && \
      test -f /Users/dom/Projects/dom/seamount/docs/adr/index.md && \
      ! grep -lr "2026-MM-DD" /Users/dom/Projects/dom/photophore/docs/adr/ /Users/dom/Projects/dom/seamount/docs/adr/
    </automated>
  </verify>
  <done>
    - 2 photophore ADR files exist; `docs/adr/index.md` exists.
    - `seamount/docs/adr/index.md` exists (cross-refs only; no own ADRs).
    - Both READMEs have §"Architecture Decision Records" with relative-path cross-refs.
    - All Status dates resolved (no placeholders).
  </done>
</task>

<task type="auto">
  <name>Task 3: SP-3.3-01..03 spec amendments to thermocline/README.md + dated [0.3.1] CHANGELOG entry</name>
  <read_first>
    - /Users/dom/Projects/dom/thermocline/README.md (find §"Dispatch Signatures" and §"Receipt Signatures" sections — exact target paragraphs)
    - /Users/dom/Projects/dom/thermocline/thermocline/CHANGELOG.md (existing `## v0.3.1 (in progress)` heading at line 12; new `## [0.3.1]` entry replaces or dates this)
    - .planning/phases/04-hardening-conformance-and-v0-1-release/04-CONTEXT.md D-02 (verbatim spec amendment text — lines 56-61 — MUST be used VERBATIM, do not paraphrase)
    - .planning/phases/04-hardening-conformance-and-v0-1-release/04-RESEARCH.md §"Release Script + CHANGELOGs" lines 892-908 (thermocline ## [0.3.1] CHANGELOG entry template)
    - .planning/phases/03-photophore-dispatch-seamount-upgrade-the-integration-phase/03-03-SUMMARY.md (find SP-3.3-01..03 entries — Phase 3 SUMMARY called these coordinator-internal; CONFLICT-02 reversal must be acknowledged in CHANGELOG)
  </read_first>
  <behavior>
    - `thermocline/README.md` §"Dispatch Signatures" has the SP-3.3-02 normative paragraph (dispatch_signature pre-fill ordering) added VERBATIM from D-02.
    - `thermocline/README.md` §"Receipt Signatures" has the SP-3.3-01 paragraph (receipt sig canonicalization invariant) AND the SP-3.3-03 paragraph (sig/bytes_hex tolerance) added VERBATIM from D-02.
    - `thermocline/thermocline/CHANGELOG.md` has `## [0.3.1] - <today's UTC date>` heading replacing or dating the existing `## v0.3.1 (in progress)` heading; section lists the three spec amendments and notes the CONFLICT-02 reclassification.
    - `SUPPORTED_VERSIONS` in `thermocline/python/src/thermocline/version.py` line 16 already includes `"0.3.1"` (verified in RESEARCH); NO version code edit needed.
  </behavior>
  <files>
    thermocline/README.md,
    thermocline/thermocline/CHANGELOG.md
  </files>
  <action>
1. **Edit `thermocline/README.md`** — locate §"Dispatch Signatures" section (use grep to find exact heading). Append a new paragraph (or insert after the existing normative paragraph if one exists):

> **SP-3.3-02 (v0.3.1) — Field pre-fill ordering**: Implementations MUST populate all non-`sig` fields of `dispatch_signature` (`signer`, `key_scheme`, `ts`, `algo`) BEFORE canonicalization and signing. The `sig` field SHALL be the empty string `""` during canonicalization. Failure to pre-fill any field will produce a signature that the verifier cannot reproduce.

2. **Edit `thermocline/README.md`** — locate §"Receipt Signatures" section. Append:

> **SP-3.3-01 (v0.3.1) — Canonicalization invariant**: When verifying a `receipt_signature`, implementations MUST canonicalize the envelope with the `receipt_signature.sig` field set to the empty string `""`, NOT removed. The signer SHALL produce the signature over this same canonicalization shape. Removing the field would cause map-key set divergence between signer and verifier.
>
> Example:
>
> ```json
> // Before signing / verification canonicalization:
> { "...envelope...": "...", "receipt_signature": { "signer": "...", "key_scheme": "brine", "ts": "...", "algo": "ed25519", "sig": "" } }
> ```
>
> **SP-3.3-03 (v0.3.1) — Field tolerance**: Verifiers SHOULD accept `receipt_signature.sig` as either a hex-encoded string (preferred) or a `bytes_hex` field carrying the same value. Implementations writing receipts MUST emit `sig`; reading them MUST accept both for backward compatibility with pre-0.3.1 drafts.

3. **Edit `thermocline/thermocline/CHANGELOG.md`** — replace the existing `## v0.3.1 (in progress — Phase 1 + Phase 2)` heading with `## [0.3.1] - <today's UTC date>` (use Keep-a-Changelog dated form). Then add `## [0.1.0] - <today's UTC date>` BELOW the new 0.3.1 section. Per PATTERNS.md cross-file invariant 9 + RESEARCH.md §"Open Questions (RESOLVED)" Q5: Keep-a-Changelog is newest-on-top by date; ties broken by version number — `0.3.1` > `0.1.0` numerically, so `## [0.3.1]` sits ABOVE `## [0.1.0]`. Final ordering top-to-bottom:

1. `## [0.3.1] - <today>` (top — spec patches landing today)
2. `## [0.1.0] - <today>` (middle — suite milestone placeholder, filled by Task 5)
3. `## [0.3.0-draft]` (bottom — existing Phase 1/2 in-progress narrative preserved)

Use RESEARCH §"Release Script + CHANGELOGs" lines 892-908 as the template for the new spec-amendments subsection within `## [0.3.1]`:

```markdown
## [0.3.1] - 2026-MM-DD

### Spec amendments (SP-3.3-01..03 — Phase 3 surfaced; Phase 4 promotes to README)

- **SP-3.3-01 — Receipt-signature canonicalization invariant**: README §"Receipt Signatures" amended with normative paragraph requiring verifiers to canonicalize the envelope with `receipt_signature.sig = ""` (empty string), NOT removed. Eliminates cross-impl reproduction-by-reverse-engineering on the receipt path.
- **SP-3.3-02 — Dispatch_signature pre-fill ordering**: README §"Dispatch Signatures" amended. All non-`sig` fields MUST be populated BEFORE canonicalization+signing.
- **SP-3.3-03 — Receipt field tolerance**: README §"Receipt Signatures" amended. Verifiers SHOULD accept `sig` OR `bytes_hex`; emitters MUST use `sig`.

> Phase 3 03-03-SUMMARY initially classified these as coordinator-internal changes (the spec was right; the coordinator was wrong). Phase 4 reclassifies them as spec clarifications because any non-Python implementation will reverse-engineer the Python coordinator to discover them. Shipping as README amendments is the cross-impl-spec-patch pattern established by THERMO-01.

### Implemented (Phase 1 + Phase 2 inheritance)

(... preserve existing in-progress narrative entries; keep them under this heading ...)

## [0.1.0] - 2026-MM-DD

(Filled in by Task 5 — suite milestone CHANGELOG entry.)
```

(Preserve the existing CHANGELOG body content; only re-headline and re-order. After this task the file has `## [0.3.1]` on top, `## [0.1.0]` below it (placeholder), and any existing v0.3.0-draft narrative preserved at the bottom.)

4. **Replace `2026-MM-DD`** placeholders with today's UTC date.
  </action>
  <verify>
    <automated>
      grep -q "SP-3.3-01" /Users/dom/Projects/dom/thermocline/README.md && \
      grep -q "SP-3.3-02" /Users/dom/Projects/dom/thermocline/README.md && \
      grep -q "SP-3.3-03" /Users/dom/Projects/dom/thermocline/README.md && \
      grep -q "## \[0.3.1\] - 2026-" /Users/dom/Projects/dom/thermocline/thermocline/CHANGELOG.md && \
      grep -q "## \[0.1.0\] - 2026-" /Users/dom/Projects/dom/thermocline/thermocline/CHANGELOG.md && \
      ! grep -q "2026-MM-DD" /Users/dom/Projects/dom/thermocline/thermocline/CHANGELOG.md
    </automated>
  </verify>
  <done>
    - README contains all three SP-3.3-* normative paragraphs VERBATIM from D-02.
    - CHANGELOG has dated `## [0.3.1]` heading; CONFLICT-02 reclassification documented.
    - CHANGELOG has placeholder `## [0.1.0]` heading (filled by Task 5).
  </done>
</task>

<task type="auto">
  <name>Task 4: Install + ops docs across all three repos + 30-minute quickstart</name>
  <read_first>
    - .planning/phases/04-hardening-conformance-and-v0-1-release/04-CONTEXT.md D-05 (per-repo doc structure)
    - .planning/phases/04-hardening-conformance-and-v0-1-release/04-RESEARCH.md §"Ops Docs Scope" (lines 338-407 — quickstart command sequence + 10-section structure + macOS gotchas)
    - /Users/dom/Projects/dom/photophore/python/src/photophore/cli/audit_cmds.py (VERIFY: no `archive` subcommand exists per Open Question 3 — docs/ops.md must document only the commands that actually ship: query, export, verify)
    - /Users/dom/Projects/dom/photophore/python/src/photophore/cli/channel_cmds.py (channel commands available for ops docs)
    - /Users/dom/Projects/dom/seamount/pi-forge/README.md (pi-forge user-facing surface)
    - /Users/dom/Projects/dom/seamount/describe-forge/README.md (describe-forge surface)
    - /Users/dom/Projects/dom/seamount/conformance/README.md (conformance harness invocation pattern)
    - D-11 (Apple Silicon Secure Enclave known-limitation text for install.md)
  </read_first>
  <behavior>
    - `thermocline/docs/install.md` documents Python 3.11+ requirement, `pip install -e thermocline/python`, keystore prerequisites (macOS Keychain works out of the box; Linux libsecret; Windows Credential Manager), Apple Silicon Secure Enclave as v0.2 known limitation (D-11).
    - `thermocline/docs/ops.md` is a placeholder ("library has no ops surface; see photophore for audit + channel ops").
    - `thermocline/docs/quickstart.md` follows the 10-section structure from RESEARCH §"Ops Docs Scope" with the verbatim command sequence; targets <30 min on macOS clean clone; includes the Keychain first-prompt gotchas.
    - `thermocline/docs/index.md` cross-links install/ops/quickstart.
    - `photophore/docs/install.md` — depends on `thermocline-py`; `pip install -e ../thermocline/thermocline/python && pip install -e .`; keystore prereqs.
    - `photophore/docs/ops.md` — documents EXISTING commands only: `photophore audit query --channel`, `photophore audit export`, `photophore audit verify`, `photophore channel new/list/show/suspend/close/set-ceiling/register-pubkey`. Open Question 3 resolution: `archive` does NOT exist per RESEARCH verification (line 1442); do NOT document it. Add a note: "Chain archival is deferred to v0.2; v0.1 ships query/export/verify."
    - `photophore/docs/index.md` cross-links.
    - `seamount/pi-forge/docs/install.md` — keystore init walkthrough; `--keyring-service seamount.piforge` namespace; pi-forge serve.
    - `seamount/describe-forge/docs/install.md` — same as pi-forge with `seamount.describeforge`; tier-1 shadow contract reminder.
    - `seamount/conformance/docs/install.md` — running `python -m forge_conformance --target URL --role <pi-forge|describe-forge>` against an arbitrary forge URL.
    - All four `install.md` files include Apple Silicon Secure Enclave note as v0.2 known limitation (D-11).
  </behavior>
  <files>
    thermocline/docs/install.md,
    thermocline/docs/ops.md,
    thermocline/docs/quickstart.md,
    thermocline/docs/index.md,
    photophore/docs/install.md,
    photophore/docs/ops.md,
    photophore/docs/index.md,
    seamount/pi-forge/docs/install.md,
    seamount/describe-forge/docs/install.md,
    seamount/conformance/docs/install.md
  </files>
  <action>
1. **Create `thermocline/docs/install.md`** (~200 lines):
   - Section 1: Prerequisites (Python 3.11+, macOS 12+ recommended for first-class keystore; Linux libsecret + D-Bus session; Windows Credential Manager).
   - Section 2: Install (`uv venv && uv pip install -e thermocline/python[dev]` OR `python -m venv .venv && pip install -e thermocline/python[dev]`).
   - Section 3: Verify (`python -c "from thermocline import canonicalize, BrineProvider; print('ok')"`).
   - Section 4: Keystore prereqs (`python-keyring` backend per platform; first-run Keychain prompt on macOS).
   - Section 5: Known limitations — Apple Silicon Secure Enclave hardware-anchored entries deferred to v0.2 (D-11 verbatim text from CONTEXT line 232: *"Default python-keyring macOS Keychain entries are software-backed (encrypted at rest, gated by the user's login session). Hardware-anchored Secure Enclave entries require Apple Silicon and a developer signing identity, and are deferred to v0.2. The v0.1 threat model is satisfied without Secure Enclave: key material never leaves the keystore."*).

2. **Create `thermocline/docs/ops.md`** (minimal placeholder, ~20 lines):
```markdown
# Thermocline-py Operations

`thermocline-py` is a library — it has no daemon, no socket, no service. There
is no thermocline ops surface.

For ops concerns affecting the suite:

- Audit log: see [photophore/docs/ops.md](../../photophore/docs/ops.md)
- Channel state: see [photophore/docs/ops.md](../../photophore/docs/ops.md)
- Forge serve: see [seamount/pi-forge/docs/install.md](../../seamount/pi-forge/docs/install.md) and [seamount/describe-forge/docs/install.md](../../seamount/describe-forge/docs/install.md)
```

3. **Create `thermocline/docs/quickstart.md`** (the CONF-07 deliverable). Use RESEARCH §"Ops Docs Scope" lines 344-407 as the source of truth for the section structure + command sequence. 10 sections:
   1. Prerequisites (Python 3.11+, macOS 12+)
   2. Clone (sibling-clone layout under `~/Projects/dom/`)
   3. Install (uv-recommended; pip alternative)
   4. First-time keystore setup (per-forge, sovereign)
   5. Start a forge (with `PIFORGE_READY port=5117` marker)
   6. Create a channel (TOFU explanation + Keychain prompt)
   7. Dispatch a task (happy path, including `photophore dispatch --channel <id> --task examples/...`)
   8. Inspect the audit log (`audit query`, `audit export > audit.jsonl`)
   9. Cleanup (channel close, optional)
   10. Next steps (links to per-repo docs/install.md, docs/ops.md, ADRs)

   Include the macOS first-prompt gotchas section (RESEARCH lines 389-392):
   - First `pi-forge init` triggers Keychain prompt "pi-forge wants to access seamount.piforge".
   - "Always Allow" required for repeated dispatches.
   - Homebrew Python or pyenv Python signing requirements.

   Use full code blocks per RESEARCH lines 343-385 (verbatim command sequence).

   Target: <30 min on clean macOS box with Python 3.11.

4. **Create `thermocline/docs/index.md`**:
```markdown
# Thermocline Documentation

- [Install](install.md) — Python toolchain + keystore prerequisites
- [Operations](ops.md) — library has no ops surface; cross-refs
- [Quickstart](quickstart.md) — clone → first dispatch → audit query (30 min)
- [ADRs](adr/index.md) — Architecture Decision Records
```

5. **Create `photophore/docs/install.md`** — same shape as thermocline install but adapted (depends on thermocline-py; `pip install -e ../thermocline/thermocline/python && pip install -e .`; same keystore prereqs; same Secure Enclave known-limitation text).

6. **Create `photophore/docs/ops.md`** — document EXISTING commands ONLY:
```markdown
# Photophore Operations

## Audit log

- `photophore audit query --channel <id>` — list entries (CLI-02 / AUDIT-05).
- `photophore audit export > audit.jsonl` — export full log as JSONL (AUDIT-06).
- `photophore audit verify` — verify chain integrity end-to-end.

## Channels

- `photophore channel new --remote-node <id> --ceiling tier-1 --key-scheme brine` — propose.
- `photophore channel list` — show all channels and states.
- `photophore channel show <id>` — show single channel detail.
- `photophore channel suspend <id> --reason "..."` — suspend a channel.
- `photophore channel close <id> --reason "..."` — close (terminal).
- `photophore channel set-ceiling <id> --ceiling tier-0` — lower the trust ceiling.
- `photophore channel register-pubkey <id> --fetch-pubkey-from <url>` — TOFU pubkey fetch.

## Known limitations (v0.1)

- **Chain archival (`audit archive`)** is deferred to v0.2. v0.1 ships query, export, and verify only. Operationally, audit logs grow append-only; periodic export + offsite-backup is the recommended workflow until archival ships.
- **Channel ceiling raising** requires a deliberate human action (CHAN-03); the audit entry records this as a distinct event type.
```

7. **Create `photophore/docs/index.md`** cross-linking install/ops/adrs.

8. **Create `seamount/pi-forge/docs/install.md`** — pi-forge install + `python -m pi_forge init --keyring-service seamount.piforge` walkthrough; serve command + PIFORGE_READY marker contract; size-limit deferral note (AT-E2 per Plan 04-01 Task 5).

9. **Create `seamount/describe-forge/docs/install.md`** — similar; tier-1 shadow contract reminder (the forge accepts tier-1 shadows and returns templated descriptions).

10. **Create `seamount/conformance/docs/install.md`**:
```markdown
# Seamount Conformance Harness — Install

The conformance harness validates any forge against the Seamount spec checklist.

## Install

```bash
cd seamount/conformance
pip install -e .[dev]
```

## Run against a forge

```bash
python -m forge_conformance --target http://localhost:5117 --role pi-forge
python -m forge_conformance --target http://localhost:5118 --role describe-forge
```

Exits 0 on full pass; non-zero with a per-check pass/fail/skip table on any failure.
```
  </action>
  <verify>
    <automated>
      test -f /Users/dom/Projects/dom/thermocline/docs/install.md && \
      test -f /Users/dom/Projects/dom/thermocline/docs/ops.md && \
      test -f /Users/dom/Projects/dom/thermocline/docs/quickstart.md && \
      test -f /Users/dom/Projects/dom/photophore/docs/install.md && \
      test -f /Users/dom/Projects/dom/photophore/docs/ops.md && \
      test -f /Users/dom/Projects/dom/seamount/pi-forge/docs/install.md && \
      test -f /Users/dom/Projects/dom/seamount/describe-forge/docs/install.md && \
      test -f /Users/dom/Projects/dom/seamount/conformance/docs/install.md && \
      grep -q "PIFORGE_READY" /Users/dom/Projects/dom/thermocline/docs/quickstart.md && \
      grep -q "Secure Enclave" /Users/dom/Projects/dom/thermocline/docs/install.md && \
      grep -q "audit archive" /Users/dom/Projects/dom/photophore/docs/ops.md && \
      grep -q "deferred to v0.2" /Users/dom/Projects/dom/photophore/docs/ops.md
    </automated>
  </verify>
  <done>
    - 10 doc files created with the correct content shape.
    - Quickstart walks the verbatim 8-step command sequence from RESEARCH.
    - `audit archive` is documented as v0.2-deferred (Open Question 3 resolution).
    - Secure Enclave is documented as v0.2 known limitation in install docs (D-11).
  </done>
</task>

<task type="auto">
  <name>Task 5: Release script + 3 CHANGELOG files (photophore new, seamount new, thermocline [0.1.0] section fill)</name>
  <read_first>
    - .planning/phases/04-hardening-conformance-and-v0-1-release/04-RESEARCH.md §"Release Script + CHANGELOGs" (lines 726-908 — full bash script body + CHANGELOG template + per-repo Implemented section)
    - .planning/phases/04-hardening-conformance-and-v0-1-release/04-CONTEXT.md D-06 (release coordination decision + CHANGELOG format)
    - /Users/dom/Projects/dom/thermocline/thermocline/CHANGELOG.md (post-Task-3 state — has `## [0.3.1]` dated AND a placeholder `## [0.1.0]` header from Task 3)
    - .planning/REQUIREMENTS.md (per-repo Implemented section: thermocline THERMO-01..07 + IDENT-01..05; photophore CHAN/AUDIT/CLASS/SHADOW/POLICY/DISP/CLI; seamount FORGE-01..05; suite CONF-01..08)
    - .planning/phases/04-hardening-conformance-and-v0-1-release/04-01-PLAN.md (Task 5 CONFLICT-04 fallback — if mypy --strict on forges was deferred, this Task adds it to seamount Known limitations)
  </read_first>
  <behavior>
    - `thermocline/scripts/tag-v0.1.0.sh` exists; mode `755`; uses `set -euo pipefail`; supports `--dry-run`; reads `$THERMOCLINE_SUITE_ROOT` (default `$HOME/Projects/dom`); 5 phases (preconditions → pre-tag lint sweep → test suite → tag (or dry-run print) → manual-push reminder).
    - `photophore/CHANGELOG.md` NEW with `## [0.1.0] - <today>` in Keep-a-Changelog-lite format.
    - `seamount/CHANGELOG.md` NEW with same shape.
    - `thermocline/thermocline/CHANGELOG.md` `## [0.1.0]` placeholder section (from Task 3) filled in with suite milestone content.
    - All three `## [0.1.0]` sections include: Added (what's new), Implemented (requirement IDs), Deferred (v0.2+ features), Known limitations (Secure Enclave, etc.).
    - If Plan 04-01 Task 5 ended up with the CONFLICT-04 fallback (mypy --strict on forges deferred), `seamount/CHANGELOG.md` Known limitations entry includes "mypy --strict on forges deferred to v0.2".
  </behavior>
  <files>
    thermocline/scripts/tag-v0.1.0.sh,
    photophore/CHANGELOG.md,
    seamount/CHANGELOG.md,
    thermocline/thermocline/CHANGELOG.md
  </files>
  <action>
1. **Create `thermocline/scripts/`** directory: `mkdir -p /Users/dom/Projects/dom/thermocline/scripts`.

2. **Create `thermocline/scripts/tag-v0.1.0.sh`** — use RESEARCH §"Release Script + CHANGELOGs" lines 728-844 VERBATIM (110-line bash script). Adjust the test suite paths for each repo:
   - thermocline: `cd thermocline/python && pytest -q`
   - photophore: `cd python && pytest -q --ignore=tests/integration` (integration tests need running forges; quickstart documents)
   - seamount: per-forge `pytest -q`; conformance harness `pytest -q`

   Also include the pre-tag lint sweep step (RESEARCH §"specifics" line 406-408): runs `python tools/ast_lint_no_print.py` + `python tools/at_coverage.py` in each repo before pytest.

   Add `at_coverage_total.py` + `property_coverage.py` (with `PROPERTY_COVERAGE_STRICT=1` env) invocations from thermocline ONLY, after per-repo gates pass. This is REQUIRED — per Plan 04-01 Task 7 (W6 revision), the cross-repo 17/17 roll-up (`at_coverage_total.py`) is intentionally NOT in any repo's CI because it needs sibling checkouts; `tag-v0.1.0.sh` is the only pre-flight check that exercises it. If `at_coverage_total.py` exits non-zero, the script HALTs before tagging.

   After 5 phases, print the manual-push reminder for each repo.

   Run `chmod +x thermocline/scripts/tag-v0.1.0.sh`.

3. **Test the script with `--dry-run`**: `bash thermocline/scripts/tag-v0.1.0.sh --dry-run` MUST exit 0 with `[DRY] git tag -a v0.1.0 ...` lines for each of the three repos AND NOT actually create git tags. This is part of the verification.

4. **Create `photophore/CHANGELOG.md`** — Keep-a-Changelog-lite template per RESEARCH §"Release Script + CHANGELOGs" lines 855-884:
```markdown
# Changelog

All notable changes to Photophore are documented here. The format is a lite
variant of [Keep a Changelog](https://keepachangelog.com/en/1.1.0/); semantic
versioning per [SemVer](https://semver.org/spec/v2.0.0.html).

## [0.1.0] - 2026-MM-DD

### Added

- Photophore reference implementation (`photophore/python/`) shipping channels,
  audit log, classifier, shadow generator, policy authoring, dispatch coordinator,
  and full CLI surface.
- New `cli.invoked` audit-entry kind (CLI-06); every CLI subcommand emits an
  invocation entry via `@audit_cli_invocation`.
- `SensitiveFilter` privacy-aware logging filter (CONF-06).
- 6 AT-A* negative tests in `python/tests/at_negative/` (CONF-02).
- Property tests: classifier default, audit chain integrity, shadow uniqueness
  (CONF-03), all at `max_examples=200`.
- New dispatch-integrated shadow uniqueness property test.

### Implemented

- **CHAN-01..06** — Channel lifecycle, trust store, ceiling monotonicity.
- **AUDIT-01..08** — Append-only SQLite audit log with BLAKE3 chain (`algo_version="blake3-v1"`).
- **CLASS-01..06** — Explicit tag + path rule + rule-based classifier; default LOCAL.
- **SHADOW-01..06** — Per-content-type abstractions; UUIDv4 IDs over `os.urandom`; no caching.
- **POLICY-01..03** — `result_policy` authoring from channel + envelope draft.
- **DISP-01..06** — 9-step dispatch coordinator; AST-lint network isolation.
- **CLI-01..07** — Full CLI surface including new CLI-06 audit retrofit + CLI-07 (tier, reason) error messages.
- **CONF-01..04, CONF-06** — Conformance, AT-* coverage, property tests, CI gates, Sensitive[T] + print-lint + SensitiveFilter.

### Deferred to subsequent milestones

- Job envelopes + per-step shadow generation (Photophore spec v0.2)
- Manifest-embedded `result_policy` authoring (v0.2)
- Ring 2 reconciliation protocol (v0.2)
- Trust score algorithm + model-based classifier (v0.3)
- Multi-hop channels + Ring 3 anchoring (v0.4)
- Per-content trust overrides (v0.5)
- Channel negotiation protocol (v1.0)
- Chain archival (`photophore audit archive`) — v0.1 ships query/export/verify only
- Daemon mode — v0.1 is per-invocation only

### Known limitations

- Default `python-keyring` macOS Keychain entries are software-backed
  (encrypted at rest, gated by user's login session). Hardware-anchored Apple
  Silicon Secure Enclave entries require a developer signing identity; deferred
  to v0.2. The v0.1 threat model is satisfied without Secure Enclave: key
  material never leaves the keystore.
- Linux + Windows ops paths documented best-effort; CI-tested matrix only
  covers `ubuntu-latest` (non-keystore) + `macos-latest` (keystore).
- AT-A5 trust-store tamper-detector is `pytest.skip()` for v0.1; defense is the
  three-store separation (CHAN-04 + ADR-0001).
```

(Replace `2026-MM-DD` with today's UTC date.)

5. **Create `seamount/CHANGELOG.md`** — same Keep-a-Changelog-lite template. Implemented section: FORGE-01..05 + suite-wide CONF-01..08 reference. Added section: `pi-forge` upgrade to real brine signing, `describe-forge` new tier-1-aware forge, `forge_conformance` cross-suite harness, 5 AT-E* negative tests. Known limitations: AT-E2 size-limit deferral note (if Plan 04-01 Task 5 chose option (b) for that test); if Plan 04-01 Task 5 ended up with CONFLICT-04 fallback (option b), add: "**mypy --strict on forges** deferred to v0.2; v0.1 runs ruff + pytest as primary type guards." Otherwise: "mypy --strict enabled on both forges in v0.1."

6. **Fill in `thermocline/thermocline/CHANGELOG.md` `## [0.1.0]` section** (created with placeholder in Task 3). Replace the placeholder block with full Keep-a-Changelog-lite content. Implemented section: THERMO-01..07, IDENT-01..05; CONF-* applicable. Note: `thermocline@5c0d87c` (cirdan→thermocline patch, pre-Phase-1) gets noted in this CHANGELOG under §"Implemented" THERMO-01 entry.

   Final ordering in `thermocline/thermocline/CHANGELOG.md` (Keep-a-Changelog newest-on-top by date; tie broken by version number per PATTERNS.md cross-file invariant 9 + RESEARCH.md §"Open Questions (RESOLVED)" Q5 — `0.3.1` > `0.1.0`): `## [0.3.1]` (top — spec patches) → `## [0.1.0]` (middle — suite milestone) → existing Phase 1/2 in-progress notes (`## [0.3.0-draft]`) preserved at the bottom.
  </action>
  <verify>
    <automated>
      test -x /Users/dom/Projects/dom/thermocline/scripts/tag-v0.1.0.sh && \
      bash /Users/dom/Projects/dom/thermocline/scripts/tag-v0.1.0.sh --dry-run 2>&1 | grep -q "DRY" && \
      ! git -C /Users/dom/Projects/dom/thermocline tag --list | grep -q "^v0.1.0$" && \
      grep -q "^## \[0.1.0\] - 2026-" /Users/dom/Projects/dom/photophore/CHANGELOG.md && \
      grep -q "^## \[0.1.0\] - 2026-" /Users/dom/Projects/dom/seamount/CHANGELOG.md && \
      grep -q "^## \[0.1.0\] - 2026-" /Users/dom/Projects/dom/thermocline/thermocline/CHANGELOG.md && \
      grep -q "Deferred to subsequent milestones" /Users/dom/Projects/dom/photophore/CHANGELOG.md && \
      grep -q "Known limitations" /Users/dom/Projects/dom/photophore/CHANGELOG.md && \
      grep -q "Secure Enclave" /Users/dom/Projects/dom/photophore/CHANGELOG.md
    </automated>
  </verify>
  <done>
    - `tag-v0.1.0.sh` exists and is executable; `--dry-run` exits 0 without creating tags.
    - All three repos have CHANGELOG.md with dated `## [0.1.0]` section in K-a-C-lite format.
    - Implemented sections reference REQUIREMENTS.md IDs accurately per repo.
    - Known limitations sections honest about what's deferred (Secure Enclave, archival, etc.).
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 6: HUMAN-ACTION — Execute tag-v0.1.0.sh real-mode AND git push --tags in each repo</name>
  <what-built>
Plan 04-01 + Plan 04-02 Tasks 1-5 have shipped all artifacts. CI is green on all three repos. CHANGELOGs are dated. The release script `thermocline/scripts/tag-v0.1.0.sh` has been tested in `--dry-run` mode and exits cleanly.

What remains is the irreducibly-manual final step: actually creating the three git tags and pushing them. Per D-06 + the suite's trust-is-never-automated principle, the operator MUST initiate the tagging.
  </what-built>
  <how-to-verify>
1. **Ensure all three working trees are clean and on `main`**:
   ```bash
   cd /Users/dom/Projects/dom/thermocline && git status && git rev-parse --abbrev-ref HEAD
   cd /Users/dom/Projects/dom/photophore && git status && git rev-parse --abbrev-ref HEAD
   cd /Users/dom/Projects/dom/seamount && git status && git rev-parse --abbrev-ref HEAD
   ```
   Each should show "working tree clean" and "main".

2. **Verify GitHub Actions are green on `origin/main`** for all three repos (Pitfall 8 — never tag pre-CI-green):
   ```bash
   gh run list --repo graywhale/thermocline --limit 1 --json conclusion --jq '.[].conclusion'
   gh run list --repo graywhale/photophore --limit 1 --json conclusion --jq '.[].conclusion'
   gh run list --repo graywhale/seamount --limit 1 --json conclusion --jq '.[].conclusion'
   ```
   All three must report "success" (NOT "in_progress", NOT "failure"). If any are not green, STOP and resolve before proceeding.

3. **Run the release script in real mode**:
   ```bash
   bash /Users/dom/Projects/dom/thermocline/scripts/tag-v0.1.0.sh
   ```
   It runs 4 phases: precondition checks → pre-tag lint sweep → test suite → tag. On success, it prints a summary and the manual `git push --tags` reminder for each repo.

4. **Push tags manually** (the script does NOT auto-push per D-06):
   ```bash
   cd /Users/dom/Projects/dom/thermocline && git push --tags
   cd /Users/dom/Projects/dom/photophore && git push --tags
   cd /Users/dom/Projects/dom/seamount && git push --tags
   ```

5. **Verify tags exist locally + remote**:
   ```bash
   for repo in thermocline photophore seamount; do
     cd /Users/dom/Projects/dom/$repo
     git tag -v v0.1.0 2>&1 | head -5
     git ls-remote --tags origin v0.1.0
   done
   ```
   Each should show the tag with the identical annotation message "v0.1.0 — coordinated with thermocline v0.1.0 + photophore v0.1.0 + seamount v0.1.0".

6. **Stop conditions** (Pitfall 8):
   - If any precondition fails in step 1, STOP. Investigate before retrying.
   - If GitHub Actions are not all green in step 2, STOP.
   - If the script aborts mid-run, the tags created so far are LOCAL ONLY; resolve the cause, delete partial tags (`git tag -d v0.1.0` per affected repo), and retry.
  </how-to-verify>
  <resume-signal>
Type "tagged-and-pushed" with the three tag SHAs to confirm completion:
```
tagged-and-pushed
thermocline: <sha>
photophore: <sha>
seamount: <sha>
```
Or describe any issues encountered.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 7: STATE.md + PROJECT.md + ROADMAP.md post-release evolution</name>
  <read_first>
    - .planning/STATE.md (current state — Phase 3 complete, Phase 4 in progress)
    - .planning/PROJECT.md §"Key Decisions" table (Phase 4 adds: 7 ADRs, SP-3.3-01..03 spec amendments, release script, CHANGELOG format)
    - .planning/ROADMAP.md (mark Phase 4 complete)
    - /Users/dom/Projects/dom/thermocline/scripts/tag-v0.1.0.sh dry-run output (capture the SHA of each repo at tag time for record)
  </read_first>
  <behavior>
    - `.planning/STATE.md` updated: Phase 4 marked complete; `Progress: completed_phases: 4 / total_phases: 4 / percent: 100`; `last_activity` set to today; `stopped_at` set to "v0.1 released"; blockers cleared (Secure Enclave moved to PROJECT.md "Pending Validations"; conformance fixture freeze noted as resolved).
    - `.planning/PROJECT.md` Key Decisions table extended with Phase 4 entries (7 ADRs landing locations, SP-3.3-01..03, release helper, CHANGELOG format).
    - `.planning/ROADMAP.md` Phase 4 marked `[x]`; Progress table updated; status column shows "Complete <today>".
    - Three v0.1.0 tags referenced in STATE.md "Recent decisions" with their commit SHAs.
  </behavior>
  <files>
    .planning/STATE.md,
    .planning/PROJECT.md,
    .planning/ROADMAP.md
  </files>
  <action>
1. **Edit `.planning/STATE.md`**:
   - Update frontmatter: `status: complete`, `stopped_at: "v0.1 released"`, `last_updated: <today>`, `progress.completed_phases: 4`, `progress.completed_plans: 12` (10 + Phase 4's 2).
   - §"Current Position": Phase 4 / Status: Complete / Last activity: <today>.
   - §"Progress" bar: 100%.
   - §"Accumulated Context > Decisions" — add new entries:
     - `- Phase 4: 7 MADR-lite ADRs landed (5 thermocline + 2 photophore + 0 seamount cross-refs-only per D-03).`
     - `- Phase 4: SP-3.3-01..03 spec patches shipped as thermocline/README.md amendments at v0.3.1 (CONFLICT-02 reclassification from Phase 3 coordinator-internal to spec-level).`
     - `- Phase 4: Three coordinated v0.1.0 git tags landed in thermocline, photophore, seamount (SHAs per tag-v0.1.0.sh run output).`
   - §"Blockers/Concerns" — remove all three Phase 4 blockers (Apple Silicon Secure Enclave → moved to PROJECT.md Pending Validations; conformance fixture provenance → resolved Phase 4 MANIFEST backfill; SP-3.3-01..03 → resolved as spec amendments).
   - §"Session Continuity" — set to v0.1 released summary.

2. **Edit `.planning/PROJECT.md`** §"Key Decisions" table — append new rows:
   - `Phase 4 | 2026-MM-DD | 7 ADRs in MADR-lite format (5 thermocline + 2 photophore); README cross-references; no symlinks. | D-03 |`
   - `Phase 4 | 2026-MM-DD | SP-3.3-01..03 spec amendments to thermocline/README.md at v0.3.1; reclassified from Phase 3 coordinator-internal. | D-02 + CONFLICT-02 |`
   - `Phase 4 | 2026-MM-DD | Release coordination via thermocline/scripts/tag-v0.1.0.sh with --dry-run; manual git push --tags. | D-06 |`
   - `Phase 4 | 2026-MM-DD | CHANGELOG format: Keep-a-Changelog-lite (Added / Implemented / Deferred / Known limitations); independent per repo. | D-06 |`

   §"Pending Validations" — add: `Apple Silicon Secure Enclave hardware-anchored keystore entries (v0.2 milestone)`.

3. **Edit `.planning/ROADMAP.md`** §Phase Details > Phase 4:
   - Change `- [ ] **Phase 4: ...**` to `- [x] **Phase 4: ...** (completed 2026-MM-DD)` at the top of the §Phases list.
   - Plans list: change both `- [ ] 04-01: ...` and `- [ ] 04-02: ...` to `- [x] ...`.
   - §Progress table: Phase 4 row → Plans Complete `2/2`, Status `Complete`, Completed `<today>`.

4. **Replace all `2026-MM-DD`** placeholders with the actual completion date (from `tag-v0.1.0.sh` output capture).
  </action>
  <verify>
    <automated>
      grep -q "completed_phases: 4" /Users/dom/Projects/dom/thermocline/.planning/STATE.md && \
      grep -q "v0.1 released" /Users/dom/Projects/dom/thermocline/.planning/STATE.md && \
      grep -q "SP-3.3-01..03" /Users/dom/Projects/dom/thermocline/.planning/PROJECT.md && \
      grep -q "^- \[x\] \*\*Phase 4" /Users/dom/Projects/dom/thermocline/.planning/ROADMAP.md && \
      grep -q "Apple Silicon Secure Enclave" /Users/dom/Projects/dom/thermocline/.planning/PROJECT.md && \
      ! grep -q "2026-MM-DD" /Users/dom/Projects/dom/thermocline/.planning/STATE.md && \
      ! grep -q "2026-MM-DD" /Users/dom/Projects/dom/thermocline/.planning/PROJECT.md && \
      ! grep -q "2026-MM-DD" /Users/dom/Projects/dom/thermocline/.planning/ROADMAP.md
    </automated>
  </verify>
  <done>
    - STATE.md reports milestone complete: 4/4 phases, 12/12 plans, 100%.
    - PROJECT.md Key Decisions has Phase 4 rows; Pending Validations has Secure Enclave entry.
    - ROADMAP.md Phase 4 marked `[x]` with completion date.
    - All placeholders resolved.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Spec README → reference impl | SP-3.3-01..03 amend the normative spec; any non-Python impl reads README. |
| ADR → README cross-ref | Relative paths (no symlinks) per D-03; broken refs would mislead readers. |
| Release script → tag side-effects | `git tag` is durable; pre-conditions enforce CI green + clean tree. |
| CHANGELOG → release verification | `tag-v0.1.0.sh` greps for dated `## [0.1.0]` heading before tagging. |
| Docs/install.md → known limitations | Honesty about Secure Enclave + archival deferrals shapes user expectations. |
| Quickstart command sequence → first-impression UX | Commands must work verbatim; broken commands break the under-30-min promise. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-11 | Repudiation | Tag created without CI green (Pitfall 8) | mitigate | `tag-v0.1.0.sh` runs full test suite + lint sweep as Phase 2 of 4 BEFORE tagging. Phase 1 asserts remote-up-to-date. |
| T-04-12 | Tampering | ADR cross-reference broken by repo move | mitigate | Relative paths (`../<repo>/docs/adr/...`) work in sibling-clone dev layout AND GitHub same-namespace clone. Verified by Task 1 + Task 2 verify steps. Documented in D-03. |
| T-04-13 | Information Disclosure | CHANGELOG omits a known limitation (e.g., classifier evasion edge case) | mitigate | Each Known-limitation section enumerated against `## Deferred Items` in STATE.md + RESEARCH.md §"Out of scope" list. Audited at Task 5. |
| T-04-14 | Spoofing | Misnamed AT-* fixtures (Pitfall 3) propagate into v0.1 release | accept | Plan 04-01 Task 3/4 documented deprecate-then-add approach; misnamed fixtures retained for cross-language port stability. CHANGELOG notes the mismatch. |
| T-04-15 | Elevation | SP-3.3-01..03 README amendments contradict Phase 3 SUMMARY | mitigate | CONFLICT-02 reversal explicitly documented in `## [0.3.1]` CHANGELOG entry (Task 3 step 3); reader of both documents sees the why. |
| T-04-16 | DoS | Release script runs on macOS BSD tools with GNU-only flag (Pitfall 5) | mitigate | RESEARCH §"Release Script" verified POSIX-only patterns; no `grep -P`, no `sed -i ''`. Task 5 verifies via dry-run. |
| T-04-17 | Tampering | Manual git push step skipped → local tags but no remote | accept | D-06 explicit: manual push is the trust-is-never-automated principle. Script PRINTS the reminder. Task 6 checkpoint walks the operator through `git push --tags`. |
| T-04-18 | Information Disclosure | quickstart.md leaks internal node id / channel id patterns | accept | Quickstart uses example IDs; no production data flows through the doc. |

</threat_model>

<verification>

## Per-task verify commands run during execution

Each task's `<verify>` block runs after that task completes.

## Whole-plan verification (run before Task 6 checkpoint)

```bash
# 1. 7 ADRs exist with MADR-lite shape
for adr in /Users/dom/Projects/dom/thermocline/docs/adr/ADR-000{1..5}-*.md \
           /Users/dom/Projects/dom/photophore/docs/adr/ADR-000{1,2}-*.md; do
  grep -q "## Context" "$adr" && grep -q "## Decision" "$adr" && grep -q "## Consequences" "$adr"
done && \

# 2. ADR cross-refs functional from each README
grep -q "../thermocline/docs/adr/ADR-0005" /Users/dom/Projects/dom/photophore/README.md && \
grep -q "../thermocline/docs/adr/ADR-0001" /Users/dom/Projects/dom/seamount/README.md && \

# 3. SP-3.3-01..03 in thermocline README
grep -q "SP-3.3-01" /Users/dom/Projects/dom/thermocline/README.md && \
grep -q "SP-3.3-02" /Users/dom/Projects/dom/thermocline/README.md && \
grep -q "SP-3.3-03" /Users/dom/Projects/dom/thermocline/README.md && \

# 4. All 3 CHANGELOGs have dated [0.1.0] section
for cl in /Users/dom/Projects/dom/thermocline/thermocline/CHANGELOG.md \
          /Users/dom/Projects/dom/photophore/CHANGELOG.md \
          /Users/dom/Projects/dom/seamount/CHANGELOG.md; do
  grep -q "^## \[0.1.0\] - 2026-" "$cl"
done && \

# 5. Quickstart + per-repo install docs exist
test -f /Users/dom/Projects/dom/thermocline/docs/quickstart.md && \
test -f /Users/dom/Projects/dom/thermocline/docs/install.md && \
test -f /Users/dom/Projects/dom/photophore/docs/install.md && \

# 6. Release script dry-run clean
bash /Users/dom/Projects/dom/thermocline/scripts/tag-v0.1.0.sh --dry-run | grep -q DRY && \

# 7. State + roadmap reflect milestone complete (post-Task 7)
grep -q "completed_phases: 4" /Users/dom/Projects/dom/thermocline/.planning/STATE.md
```

After Task 6 checkpoint:

```bash
# 8. Three coordinated v0.1.0 tags exist
for repo in thermocline photophore seamount; do
  git -C /Users/dom/Projects/dom/$repo tag --list v0.1.0 | grep -q "^v0.1.0$"
done
```

</verification>

<success_criteria>

Plan 04-02 complete when:
- [ ] 7 ADRs exist in MADR-lite format across 2 repos (5 thermocline + 2 photophore).
- [ ] Each repo's README has §"Architecture Decision Records" with relative-path cross-refs (no symlinks).
- [ ] `thermocline/README.md` has all three SP-3.3-* normative paragraphs VERBATIM from D-02.
- [ ] `thermocline/thermocline/CHANGELOG.md` has dated `## [0.3.1]` section documenting CONFLICT-02 reclassification + dated `## [0.1.0]` section with suite milestone content.
- [ ] `photophore/CHANGELOG.md` + `seamount/CHANGELOG.md` exist with `## [0.1.0]` Keep-a-Changelog-lite content.
- [ ] All per-repo install + ops docs created; thermocline/docs/quickstart.md walks <30-min flow with verbatim command sequence.
- [ ] `tag-v0.1.0.sh` exists with `--dry-run` mode; dry-run exits 0 without creating tags.
- [ ] HUMAN-ACTION checkpoint: 3 coordinated v0.1.0 git tags created in thermocline + photophore + seamount, all with identical annotation message; pushed to remote.
- [ ] STATE.md reports `completed_phases: 4`, `percent: 100`, `stopped_at: "v0.1 released"`.
- [ ] PROJECT.md Key Decisions has Phase 4 rows; ROADMAP.md Phase 4 marked complete.

</success_criteria>

<output>
After completion, create `.planning/phases/04-hardening-conformance-and-v0-1-release/04-02-SUMMARY.md` enumerating:
- 7 ADR landings (paths + Status dates).
- SP-3.3-01..03 amendment paragraphs (location in README + CHANGELOG entry).
- All docs files created (10 paths).
- Release script + 3 CHANGELOG files (paths + first-line headings).
- Three v0.1.0 git tag SHAs (one per repo).
- STATE/PROJECT/ROADMAP final state (Phase 4 closure record).
- Any deferred items finalized into v0.2 backlog.
</output>
