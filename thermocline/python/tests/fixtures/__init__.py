"""Static-type-check and runtime fixtures for the identity-module gates.

The files in this package are loaded by tests in ``tests/`` to verify that
deliberate misuse of the identity API is rejected at the appropriate layer
(static type-check, runtime, or both).
"""
